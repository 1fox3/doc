# 事务

> 专题：MySQL
> 来源：Mysql.xmind

## 补充与实践

- 补充：InnoDB 通过 undo log 和 ReadView 实现 MVCC，通过 redo log 保证崩溃恢复，通过 binlog 支持复制和归档。
- 实践：长事务会拖住 undo 清理并影响版本链，线上应限制事务时长和交互式事务。
- 误区：可重复读不等于完全不会幻读，InnoDB 通过 next-key lock 在当前读场景解决幻读。

## 详细说明

### 是什么

- `事务` 是 `MySQL` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `事务` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `事务`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `事务` 展开，属于 `MySQL` 的复习范围。
- 建议优先掌握：特性, 状态, 开启事务, 隐式提交, 保存点。
- 二级节点提示了主要拆分角度：原子性-Atomicity, 隔离性-Isolation, 一致性-Consistency, 持久性-Durability, 活动的, 部分提交的, 失败的, 中止的, 提交的, BEGIN [WORK]。
- 三级节点通常对应具体细节、API、机制或易错点：READ ONLY, READ WRITE, WITH CONSISTENT SNAPSHOT, LOAD DATA, START SLAVE, ANALYZE TABLE, CACHE INDEX, CACHE TABLE, 添加, 回滚至保存点。

### 复习追问

- `特性` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `状态` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `开启事务` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `隐式提交` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `保存点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 事务

### 特性

#### 原子性-Atomicity

#### 隔离性-Isolation

#### 一致性-Consistency

#### 持久性-Durability

### 状态

#### 活动的

#### 部分提交的

#### 失败的

#### 中止的

#### 提交的

### 开启事务

#### BEGIN [WORK]

#### START TRANSACTION

- READ ONLY

- READ WRITE

- WITH CONSISTENT SNAPSHOT
  - 启用一致性读

### 隐式提交

#### 定义或修改数据库对象的数据定义语句

#### 隐式使用或修改mysql中的数据表

#### 事务控制或关于锁定的语句

#### 加载数据的语句

- LOAD DATA

#### 关于mysql的复制的一些语句

- START SLAVE

#### 其他一些语句

- ANALYZE TABLE

- CACHE INDEX

- CACHE TABLE

### 保存点

#### SAVEPOINT 保存点名称

- 添加

#### ROLLBACK TO 保存点名称

- 回滚至保存点

#### RELEASE SAVEPOINT 保存点名称

- 删除保存点
