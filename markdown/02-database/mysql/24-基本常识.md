# 基本常识

> 专题：MySQL
> 来源：面试内容汇总/Mysql.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `基本常识` 是 `MySQL` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `基本常识` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `基本常识`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `基本常识` 展开，属于 `MySQL` 的复习范围。
- 建议优先掌握：一个表最多建64个索引, 索引最多包含16个列, B+数的一个节点默认大小16K, 索引失效场景, count性能排序, distinct和groupby性能, 最大连接数, MyISAM和InnoDB区别。
- 二级节点提示了主要拆分角度：不符合索引的最左匹配原则, like模糊查询以%开头, 索引列上使用了函数、表达式计算, 数据类型出现隐式转化, JOIN条件中索引列数据不一致, 条件存在NULL值, 使用了!=，<>, IN,NOT IN判断条件, 查询条件中使用了or, count(*)>count(1)>count(主键)>count(字段), Mysql8.0之前，group by会做隐式排序，所以distinct性能会更好。
- 三级节点通常对应具体细节、API、机制或易错点：MyISAM, InnoDB。

### 复习追问

- `一个表最多建64个索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `索引最多包含16个列` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `B+数的一个节点默认大小16K` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `索引失效场景` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `count性能排序` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 基本常识

### 一个表最多建64个索引

### 索引最多包含16个列

### B+数的一个节点默认大小16K

### 索引失效场景

#### 不符合索引的最左匹配原则

#### like模糊查询以%开头

#### 索引列上使用了函数、表达式计算

#### 数据类型出现隐式转化

#### JOIN条件中索引列数据不一致

#### 条件存在NULL值

#### 使用了!=，<>, IN,NOT IN判断条件

#### 查询条件中使用了or

### count性能排序

#### count(*)>count(1)>count(主键)>count(字段)

### distinct和groupby性能

#### Mysql8.0之前，group by会做隐式排序，所以distinct性能会更好

#### Mysql8.0之后，删除了group by的隐式排序，因此性能几乎相同

### 最大连接数

#### 151，上限为10万

### MyISAM和InnoDB区别

#### 事务

#### 外键

#### 锁

#### MyISAM允许没有任何索引和主键，InnoDB必须要有

#### MyISAM表和索引分两个文件存储，InnoDB表和索引在一个数据文件中

- MyISAM
  - .frm
  - .myd
  - .myi

- InnoDB
  - .frm
  - .idb

### Mysql5.7+支持JSON对象和JSON数组

### Change Buffer

#### 只能用于非唯一索引

### Buffer Pool

### 数据复制方式

#### 异步复制

#### 全同步复制

#### 半同步复制（Mysql5.5+）

#### 组复制（Mysql5.7+）

### 表锁

#### 意向共享锁

#### 意向排它锁

#### 自增锁

### 行锁

#### 锁索引而不是锁记录

#### 算法

- Record Lock

- Gap Lock

- Next-Key Lock
  - 等于Record Lock+Gap Lock
