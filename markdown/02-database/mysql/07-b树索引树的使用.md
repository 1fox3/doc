# B+树索引树的使用

> 专题：MySQL
> 来源：Mysql.xmind

## 补充与实践

- 补充：MySQL 8.0 优化器会结合统计信息、直方图和成本模型选择执行计划，索引存在不代表一定会用。
- 实践：设计复合索引时按等值、范围、排序、覆盖的优先级综合考虑，同时关注字段选择性和写入成本。
- 排障：慢 SQL 分析先看 `EXPLAIN ANALYZE`、扫描行数、回表次数、临时表和文件排序。

## 脑图内容

## B+树索引树的使用

### 创建索引注意事项

#### 只为用于搜索，排序或分组的列创建索引

#### 当列中不重复值得个数在总记录条数中占比很大时，才为列建立索引

#### 索引列的类型尽量小

#### 可以只为索引列前缀创建索引，以减小索引占用的存储空间

#### 尽量使用覆盖索引进行查询，以避免回表操作带来的性能损耗

#### 让索引列以列名的形式单独出现在搜索条件中

#### 为了尽可能的让聚簇索引引发页面分裂的情况，建议让主键拥有AUTO_INCREMENT属性

#### 定位并删除表中的冗余和重复索引
