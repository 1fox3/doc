# MongoDB

> 来源：面试内容汇总/MongoDB.xmind

## 核心认知

- MongoDB 适合文档模型、灵活 schema、水平扩展和高写入吞吐场景，但不应替代所有关系型建模。
- 建模时优先围绕查询路径设计文档结构，权衡嵌入、引用、冗余和一致性成本。

## 面试重点

- BSON、集合、文档、索引、复制集、分片、WriteConcern、ReadConcern。
- 唯一索引、复合索引、TTL 索引、覆盖查询和慢查询分析。

## 实践检查点

- 能解释复制集选举、oplog 同步和主从延迟对读一致性的影响。
- 能根据高基数字段和查询条件设计复合索引顺序。

## 版本与趋势

- MongoDB 6/7/8 持续强化分片、时间序列、变更流和查询性能，建模仍应围绕访问模式设计。
- 不要把灵活 schema 等同于无 schema，生产环境需要字段规范、索引规范和容量治理。

## 脑图内容整理

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `预留特殊的数据库` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `预留特殊的数据库` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `预留特殊的数据库`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `预留特殊的数据库` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：admin, local, config, test。
- 二级节点提示了主要拆分角度：当MongoDB使用分片设置时，config数据库可用来保存分片的相关信息。

### 复习追问

- `admin` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `local` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `config` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `test` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 预留特殊的数据库

### admin

### local

### config

#### 当MongoDB使用分片设置时，config数据库可用来保存分片的相关信息

### test

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `特点` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `特点` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `特点`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `特点` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：数据记录被存储为文档, 模式自由, 支持多种查询方式, 支持ACID事务, 高效的二进制存储, 自带数据压缩功能, 只是MapReduce, 支持多种类型的索引。
- 二级节点提示了主要拆分角度：snappy压缩算法, zlib压缩算法, Zstandard压缩算法, 单文件不超过16MB，超过则使用GridFS进行存储, GridFS可将大型数据进行分块处理，然后将这些切分后的小文档保存在数据库中。

### 复习追问

- `数据记录被存储为文档` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `模式自由` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `支持多种查询方式` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `支持ACID事务` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `高效的二进制存储` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 特点

### 数据记录被存储为文档

### 模式自由

### 支持多种查询方式

### 支持ACID事务

### 高效的二进制存储

### 自带数据压缩功能

#### snappy压缩算法

#### zlib压缩算法

#### Zstandard压缩算法

### 只是MapReduce

### 支持多种类型的索引

### 支持failover

### 支持分片集群

### 支持存储大文件

#### 单文件不超过16MB，超过则使用GridFS进行存储

#### GridFS可将大型数据进行分块处理，然后将这些切分后的小文档保存在数据库中

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `存储引擎` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `存储引擎` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `存储引擎`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `存储引擎` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：WiredTiger(默认), In-Memory。
- 二级节点提示了主要拆分角度：B+数作为存储结构。

### 复习追问

- `WiredTiger(默认)` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `In-Memory` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 存储引擎

### WiredTiger(默认)

#### B+数作为存储结构

### In-Memory

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `索引` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `索引` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `索引`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `索引` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：单字段索引, 复合索引, 多键索引, 哈希索引, 文本索引, 地理位置索引, 唯一索引, TTL索引。
- 二级节点提示了主要拆分角度：遵循左前缀原则, 字段为数组, 提供了一个过期机制，允许为每一个文档设置一个过期时间，当一个文档达到预设的过期时间后就会删除, expireAfterSeconds, 运行原理, 仅支持单字段索引，_id不支持TTL索引, 上限集合(Capped Collection)不支持TTL索引，MongoDB无法从上限集合中删除文档, 不能再一个字段上建2个TTL索引。
- 三级节点通常对应具体细节、API、机制或易错点：后台进程每60秒触发一次删除任务, 副本集通过主节点的oplog来同步删除, oplog是local库下的特殊的上限集合。

### 复习追问

- `单字段索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `复合索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `多键索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `哈希索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `文本索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 索引

### 单字段索引

### 复合索引

#### 遵循左前缀原则

### 多键索引

#### 字段为数组

### 哈希索引

### 文本索引

### 地理位置索引

### 唯一索引

### TTL索引

#### 提供了一个过期机制，允许为每一个文档设置一个过期时间，当一个文档达到预设的过期时间后就会删除

#### expireAfterSeconds

#### 运行原理

- 后台进程每60秒触发一次删除任务

- 副本集通过主节点的oplog来同步删除

#### 仅支持单字段索引，_id不支持TTL索引

#### 上限集合(Capped Collection)不支持TTL索引，MongoDB无法从上限集合中删除文档

- oplog是local库下的特殊的上限集合

#### 不能再一个字段上建2个TTL索引

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `高可用` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `高可用` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `高可用`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `高可用` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：复制集群, 分片集群。
- 二级节点提示了主要拆分角度：容器分类, 分片键, 分片策略, 分片数据存储。
- 三级节点通常对应具体细节、API、机制或易错点：主节点, 从节点, 仲裁节点, Config Servers, AppServer/Router, Shard, 所有文档都出现, 必须是集合的一个索引, 大小不能超过512字节, 基于范围分片。

### 复习追问

- `复制集群` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `分片集群` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 高可用

### 复制集群

#### 容器分类

- 主节点

- 从节点

- 仲裁节点

### 分片集群

#### 容器分类 (2)

- Config Servers

- AppServer/Router

- Shard

#### 分片键

- 所有文档都出现

- 必须是集合的一个索引

- 大小不能超过512字节

#### 分片策略

- 基于范围分片

- 基于Hash值分片

#### 分片数据存储

- Chunk块
  - 最大默认64MB
  - Chunk过大会分裂
  - 均衡器(Balancer)执行自动平衡，保证各个Shard上的Chunk数量保持均衡

- 迁移
  - 发送方发起迁移
  - 接收方发起chunk拷贝
  - 接收方拉取增量修改
  - 发送方等待接收当chunk拷贝完成
  - 发送方进入临界区，写操作挂起
  - 接收方执行commit，并再一次拉取增量同步，向发送方同步完成结果，发送发推出临界区
  - ConfigServer执行commit表示迁移完成

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `instantiate` 是 `MongoDB` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `instantiate` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `instantiate`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `instantiate` 展开，属于 `MongoDB` 的复习范围。
- 建议优先掌握：1、无覆盖方法，直接实例化, 2、有覆盖方法，则生成cglib子类(动态代理)。

### 复习追问

- `1、无覆盖方法，直接实例化` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `2、有覆盖方法，则生成cglib子类(动态代理)` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## instantiate

### 1、无覆盖方法，直接实例化

### 2、有覆盖方法，则生成cglib子类(动态代理)
