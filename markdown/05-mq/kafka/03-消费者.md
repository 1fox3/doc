# 消费者

> 专题：Kafka
> 来源：Kafka.xmind

## 补充与实践

- 补充：Kafka 3.x 新集群可使用 KRaft，不再依赖 ZooKeeper；可靠性仍由 ack、ISR、副本和幂等共同决定。
- 实践：消费端必须设计幂等，offset 提交要与业务处理结果绑定，避免先提交后处理导致消息丢失。
- 排障：消费延迟从生产速率、分区热点、Rebalance、消费耗时和下游依赖五处定位。

## 详细说明

### 是什么

- `消费者` 是 `Kafka` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `消费者` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `消费者`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `消费者` 展开，属于 `Kafka` 的复习范围。
- 建议优先掌握：投递模式, 备注, 开发过程, 反序列化器, 消息消费, 位移提交, 指定位移消费。
- 二级节点提示了主要拆分角度：点对点, 发布订阅, 消息只会被消费者组中的一个消费者消费, 配置消费者客户端参数及创建相应的消费者实例, 订阅主题, 拉取消息并消费, 提交消费位移, 关闭消费者实例, Deserializer, 拉取。
- 三级节点通常对应具体细节、API、机制或易错点：重点参数配置, 配置类, 订阅, 取消订阅, 获取指定主题的元数据信息, configure, deserialize, close, poll, records。

### 复习追问

- `投递模式` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `备注` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `开发过程` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `反序列化器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `消息消费` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 消费者

### 投递模式

#### 点对点

#### 发布订阅

### 备注

#### 消息只会被消费者组中的一个消费者消费

### 开发过程

#### 配置消费者客户端参数及创建相应的消费者实例

- 重点参数配置
  - boostrap.servers
  - group.id
  - key.deserializer
  - value.deserializer

- 配置类
  - ConsumerConfig

#### 订阅主题

- 订阅
  - subscribe
    - 指定主题和主题模式
  - assign
    - 指定分区集合

- 取消订阅
  - unsubscribe
  - subscribe指定空主题
  - assign指定空分区

- 获取指定主题的元数据信息
  - partitionFor

#### 拉取消息并消费

#### 提交消费位移

#### 关闭消费者实例

### 反序列化器

#### Deserializer

- configure
  - key.deserializer.encoding
  - value.deserializer.encoding
  - deserializer.encoding

- deserialize

- close

### 消息消费

#### 拉取

- poll

- records
  - 拉取指定分区的消息

- ConsumerRecord
  - topic
    - 主题
  - partition
    - 分区
  - timestamp
    - 时间
  - timestampType
    - 创建时间或追加到日志的时间
  - headers
    - 消息头内容
  - key
    - 消息的键
  - value
    - 消息的值
  - serializedKeySize
    - key序列化后的长度
  - serializedValueSize
    - value序列化后的长度
  - checksum
    - CRC32的校验值

#### 控制消费

- pause
  - 暂停

- resume
  - 恢复

### 位移提交

#### 位移存储位置

- __consumer_offsets

#### 查看位移信息

- KafkaConsumer,position

- KafkaConsumer.committed

#### 提交位移

- commitSync

- commitAsync

### 指定位移消费

#### auto.offset.reset

- earliest

- latest

- none

#### seek

#### endOffsets

- 获取指定分区的末尾消息位置

#### beginningOffsets

- 获取指定分区的开始消息位置

#### offsetsForTimes

- 通过timestamp查询分区的位置

### 再均衡

#### ConsumerRebalanceListener

- onPartitionsRevoked
  - 再均衡开始之前和消费者停止读取消息之后被调用

- onPartitionsAssigned
  - 重新分配分区之后和消费者开始读取消息之前被调用

### 消费者拦截器

#### ConsumerInterceptor

- onConsume
  - 再poll方法返回之前调用

- onCommit
  - 提交完消费位移之后调用

- close

### 多线程实现

#### KafkaConsumer.acquire

- 用于检测当前是否有一个线程正在操作

#### KafkaConsumer.release

#### KafkaConsumer是线程安全的

### 消费者重点参数

#### fetch.min,bytes

- 每次拉取得最小数据量，默认1B

#### fetch.max.bytes

- 每次拉取得最大数据量，默认50MB

#### fetch.max.wait.ms

- 每次拉取最大等待时间，默认500ms

#### max.partition.fetch.bytes

- 每个分区返回给消费者的最大数据量，默认1MB

#### max.poll.records

- 每次拉取的最大消息数，默认500条

#### connections.max.idle.ms

- 指定在多久之后关闭限制的连接，默认9分钟

#### exclude.internal.topics

- 是否忽略内部主题
  - __consumer_offsets
  - __transaction_state

#### receive.buffer.bytes

- Socket接收消息缓冲区，默认64KB，-1则使用系统默认值

#### send.buffer.bytes

- Socket发送消息缓冲区，默认128KB，-1则使用系统默认值

#### request.timeout.ms

- Consumer等待请求响应的最长时间，默认30000ms

#### metdata.max.age.ms

- 元数据过期时间，默认5分钟

#### reconnect.backoff.ms

- 尝试重新连接指定主机之前的等待时间，默认50ms

#### retry.backoff.ms

- 尝试重新发送失败请求的等待时间，默认100ms

#### isolation.level

- 事务隔离级别
  - read_uncommitted
  - read_committed
