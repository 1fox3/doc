# 服务端

> 专题：Kafka
> 来源：Kafka.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `服务端` 是 `Kafka` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `服务端` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `服务端`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `服务端` 展开，属于 `Kafka` 的复习范围。
- 建议优先掌握：协议, 时间轮, 延时操作, 控制器。
- 二级节点提示了主要拆分角度：结构, TimingWheel, DelayQueue, 创建延时生产操作(DelayedProduce), 延时操作管理器(DelayOperationPurgatory), 优雅关闭, 分区leader的选举。
- 三级节点通常对应具体细节、API、机制或易错点：Request, Response, 集群中有且仅有一个控制器, 通过比较controller_epoch来判断是否过期, 职责, kafka-server-stop.sh, controlled.shutdown.enable=true, 步骤, 原理, 控制关闭的优点。

### 复习追问

- `协议` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `时间轮` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `延时操作` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `控制器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 服务端

### 协议

#### 结构

- Request
  - RequestHeader
    - api_key
      - API标识
    - api_version
      - API版本号
    - correlation_id
      - 由客户端指定的一个数字来唯一标识这次请求的id
    - client_id
      - 客户端id
  - RequestBody

- Response
  - RequestHeader
    - correlation_id
      - 由客户端指定的一个数字来唯一标识这次请求的id
  - RequestBody

### 时间轮

#### TimingWheel

#### DelayQueue

### 延时操作

#### 创建延时生产操作(DelayedProduce)

#### 延时操作管理器(DelayOperationPurgatory)

### 控制器

#### 控制器

- 集群中有且仅有一个控制器

- 通过比较controller_epoch来判断是否过期

- 职责
  - 监听分区相关变化
  - 监听主题相关变化
  - 监听broker相关变化
  - 从ZooKeeper中读取当前所有主题，分区以及broker有关的信息并进行相应的管理
  - 启动并管理分区状态机和副本状态机
  - 更新集群的元数据信息
  - auto.leader.rebalance.enable设置为true时，开启一个名为“auto-leader-rebalance-task”的定时任务来负责维护分区的优先副本的均衡

#### 优雅关闭

- kafka-server-stop.sh

- controlled.shutdown.enable=true

- 步骤
  - 获取Kafka的服务进程号PIDS
  - 使用kill -s TERM $PIDS 或 kill -15 $PIDS关闭进程

- 原理
  - 执行kafka-shutdown-hock关闭钩子，再执行控制关闭(ControlledShutDown)的动作

- 控制关闭的优点
  - 消息全部同步到磁盘上，下次重新上线时，减少不需要的日志恢复操作
  - 关闭服务前会进行leader副本的迁移操作，减少分区不可用的时间

#### 分区leader的选举

- OfflinePartitionLeaderElectionStategy
  - 按照AR集合中的副本的顺序选择第一个存活的副本
  - 创建分区
  - 分区上线

- RessignPartitonLeaderElectionStrategy
  - 从重分配的AR列表中找到第一个存活的副本，且这个副本再ISR列表中
  - 分区重分配

- PreferredReplicaPartitionLeaderElectonStrategy
  - 直接将优先副本设置为leader即可
  - 优限副本选举时

- ControlledShutdownPartitionLeaderElectionStrategy
  - 从AR列表中找到第一个存活的副本，且这个副本再ISR列表中
  - 控制关闭时(ControlledShutDown)
