# 生产者

> 专题：Kafka
> 来源：Kafka.xmind

## 补充与实践

- 补充：Kafka 3.x 新集群可使用 KRaft，不再依赖 ZooKeeper；可靠性仍由 ack、ISR、副本和幂等共同决定。
- 实践：消费端必须设计幂等，offset 提交要与业务处理结果绑定，避免先提交后处理导致消息丢失。
- 排障：消费延迟从生产速率、分区热点、Rebalance、消费耗时和下游依赖五处定位。

## 详细说明

### 是什么

- `生产者` 是 `Kafka` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `生产者` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `生产者`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `生产者` 展开，属于 `Kafka` 的复习范围。
- 建议优先掌握：开发过程, 序列化器, 分区器, 拦截器, 生产者整体架构。
- 二级节点提示了主要拆分角度：配置生产者客户端参数及创建相应的生产者实例, 构建待发送的消息, 发送消息, 关闭生产这实例, Serializer<T>, 接口, DefaultPartitioner, ProducerInterceptor, 架构图, 线程。
- 三级节点通常对应具体细节、API、机制或易错点：必要参数配置, 配置类, 模式, 处理流程, close, Partitioner, key不为null, key为null, onSend, onAcknowledgement。

### 复习追问

- `开发过程` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `序列化器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `分区器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `拦截器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `生产者整体架构` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 生产者

### 开发过程

#### 配置生产者客户端参数及创建相应的生产者实例

- 必要参数配置
  - bootstrap.servers
  - ket,serializer
  - value.serializer
  - retries

- 配置类
  - ProducerConfig

#### 构建待发送的消息

#### 发送消息

- 模式
  - 发后既忘(fire-and-forget)
  - 同步(sync)
  - 异步(async)

- 处理流程
  - 拦截器
  - 序列化器
  - 分区器

#### 关闭生产这实例

- close
  - 会阻塞等待之前所有的发送请求完成后再关闭生产者

### 序列化器

#### Serializer<T>

### 分区器

#### 接口

- Partitioner
  - Configurable(父接口)

#### DefaultPartitioner

- key不为null
  - 对key进行哈希(MurMurHashe2算法)

- key为null
  - 轮询

### 拦截器

#### ProducerInterceptor

- onSend
  - 对消息发送之前

- onAcknowledgement
  - 消息发送结果被应答之前

- close

### 生产者整体架构

#### 架构图

#### 线程

- 主线程
  - 将消息缓存到消息累加器

- Sender线程
  - 从消息累加器中获取并发送到kafka中

#### 消息累加器

- buffer.memory
  - 默认32M

- max.block.ms
  - 默认60000毫秒

- batch.size
  - 缓存分块大小

#### Sender线程

- max.in.flight.request.pre.connection
  - 每个连接最多缓存的请求数

#### 元数据的更新

- leastLoadedNode
  - 负载最小节点

- metadata.max.age.ms
  - 元数据刷新间隔，默认值300000毫秒，5分钟

#### 重要参数

- acks
  - 1：只要有leader副本写入成功，就返回成功
  - 0：不等待任何服务端的相应
  - -1，all：ISR中的所有副本都写入成功

- max.request.size
  - 生产者客户端能发送的消息的最大值，默认1MB

- retries
  - 生产者重试次数

- retry.backoff.ms
  - 重试间隔，默认100毫秒

- compressiion.type
  - 压缩方式

- connection.max.idle.ms
  - 在多久之后关闭限制的连接，默认9分钟

- linger.ms
  - 生产者发送ProducerBatch之前等待更多消息加入ProducerBatch时间，默认值为0

- receive.buffer.bytes
  - Socket接收消息缓冲区大小默认值32KB，-1位系统默认值

- send.buffer.bytes
  - Socket发送消息缓冲区大小默认值128KB，-1位系统默认值

- request.timeout.ms
  - Producer等待请求响应的最长时间，默认30秒
