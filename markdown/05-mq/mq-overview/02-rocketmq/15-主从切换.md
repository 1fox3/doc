# 主从切换

> 专题：消息队列总览
> 来源：面试内容汇总/MQ.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `主从切换` 是 `消息队列总览` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `主从切换` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `主从切换`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `主从切换` 展开，属于 `消息队列总览` 的复习范围。
- 建议优先掌握：目的, Raft算法, 数据存储兼容。
- 二级节点提示了主要拆分角度：当主节点宕机后，影响的是写入的能力，读能力受影响比较小, Leader选举, 日志复制, 日志编号, 日志追加和提交机制, 保持日志一直性, DLedger在整合使用DLedger条目包裹RocketMQ中的CommitLog条目，即DLedger条目的body字段用来存储整个CommitLog条目, 引入dividedCommitLogOffset变量，物理偏移量小于该值得消息存储在旧的CommitLog文件中，实现升级DLedger集群后能依然能访问旧的数据, 新DLedger集群启动后，会将最后一个CommitLog文件填充，即新的数据不会再写入原先的CommitLog文件, 消息追加到DLedger数据日志文件中，返回的偏移量不是DLedger条目的起始偏移量，而是DLedger条目中body字段的起始偏移量，即真实消息的起始偏移量，保证消息物理偏移量与RocketMQ CommitLog一致。
- 三级节点通常对应具体细节、API、机制或易错点：Follower, Candidate, Leader, 仅当超过一半容器成功存储了才返回成功, 2段式提交, 先查找从节点当前已追加的最大日志序号，用ledgerEndIndex表示, 尝试最佳LedgerEndIndex+1的日志，根据日志序号从写队列中查找日志, 如果日志不存在，从节点在响应主节点的append请求时告知数据不一致, 主节点会发送COMPARE明亮，比较主从数据差异, 补充缺失的数据或删除冗余数据，最终达到一致性。

### 复习追问

- `目的` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Raft算法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `数据存储兼容` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 主从切换

### 目的

#### 当主节点宕机后，影响的是写入的能力，读能力受影响比较小

### Raft算法

#### Leader选举

- Follower

- Candidate

- Leader

#### 日志复制

- 仅当超过一半容器成功存储了才返回成功

### 日志复制

#### 日志编号

#### 日志追加和提交机制

- 2段式提交

#### 保持日志一直性

- 先查找从节点当前已追加的最大日志序号，用ledgerEndIndex表示

- 尝试最佳LedgerEndIndex+1的日志，根据日志序号从写队列中查找日志

- 如果日志不存在，从节点在响应主节点的append请求时告知数据不一致

- 主节点会发送COMPARE明亮，比较主从数据差异

- 补充缺失的数据或删除冗余数据，最终达到一致性

### 数据存储兼容

#### DLedger在整合使用DLedger条目包裹RocketMQ中的CommitLog条目，即DLedger条目的body字段用来存储整个CommitLog条目

#### 引入dividedCommitLogOffset变量，物理偏移量小于该值得消息存储在旧的CommitLog文件中，实现升级DLedger集群后能依然能访问旧的数据

#### 新DLedger集群启动后，会将最后一个CommitLog文件填充，即新的数据不会再写入原先的CommitLog文件

#### 消息追加到DLedger数据日志文件中，返回的偏移量不是DLedger条目的起始偏移量，而是DLedger条目中body字段的起始偏移量，即真实消息的起始偏移量，保证消息物理偏移量与RocketMQ CommitLog一致
