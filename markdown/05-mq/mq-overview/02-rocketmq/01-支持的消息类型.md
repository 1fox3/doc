# 支持的消息类型

> 专题：消息队列总览
> 来源：面试内容汇总/MQ.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `支持的消息类型` 是 `消息队列总览` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `支持的消息类型` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `支持的消息类型`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `支持的消息类型` 展开，属于 `消息队列总览` 的复习范围。
- 建议优先掌握：普通消息, 顺序消息（FIFO）, 定时/延时消息（Delay）, 事务消息（Transaction）。
- 二级节点提示了主要拆分角度：最大4MB, 顺序消息的顺序关系通过消息组（MessageGroup）判定和识别, 分类, SHEDULE_TOPIC_XXX, 消息到期会写入真实Topic，让消费者消费, 最大64KB, 事务消息加事务反查机制。
- 三级节点通常对应具体细节、API、机制或易错点：全局顺序, 分区顺序。

### 复习追问

- `普通消息` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `顺序消息（FIFO）` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `定时/延时消息（Delay）` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `事务消息（Transaction）` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 支持的消息类型

### 普通消息

#### 最大4MB

### 顺序消息（FIFO）

#### 顺序消息的顺序关系通过消息组（MessageGroup）判定和识别

#### 分类

- 全局顺序
  - 对于指定的Topic，所有消息按照严格的先入先出顺序进行发布和消费

- 分区顺序
  - 对于指定的Topic，所有消息根据sharding key进行区块分区，同一个分区内的消息按照严格FIFO顺序进行发布和消费

#### 最大4MB (4)

### 定时/延时消息（Delay）

#### SHEDULE_TOPIC_XXX

#### 消息到期会写入真实Topic，让消费者消费

#### 最大64KB

### 事务消息（Transaction）

#### 事务消息加事务反查机制

#### 最大64KB (9)
