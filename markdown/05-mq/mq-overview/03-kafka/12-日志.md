# 日志

> 专题：消息队列总览
> 来源：面试内容汇总/MQ.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `日志` 是 `消息队列总览` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `日志` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `日志`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `日志` 展开，属于 `消息队列总览` 的复习范围。
- 建议优先掌握：文件划分, 根目录, 日志格式, 消息压缩, 日志索引。
- 二级节点提示了主要拆分角度：Log, __consumer_offsets-*, topic-log-*, cleaner-offset-checkpoint, log-start-offset-checkpoint, recovery-point-offset-checkpoint, replication-offset-checpoint, meta.properties, .lock, v0版本(0.10.0之前采用)。
- 三级节点通常对应具体细节、API、机制或易错点：LogSegment, offset, message size, Record, 变长字段, RecordBatch, compression.type, key为null, offset从0开始分配, 偏移量索引文件用来建立消息偏移量到物理地址之间的映射关系，方便快速定位消息所在的物理文件位置。

### 复习追问

- `文件划分` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `根目录` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `日志格式` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `消息压缩` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `日志索引` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 日志

### 文件划分

#### Log

- LogSegment
  - .log(日志文件)
  - .index(偏移量索引文件)
  - .timeindex(时间戳索引文件)
  - .deleted
  - .cleaned
  - .swap
  - .snapshot
  - .txnindex
  - leader-epoch-checkpoint

### 根目录

#### __consumer_offsets-*

#### topic-log-*

#### cleaner-offset-checkpoint

#### log-start-offset-checkpoint

#### recovery-point-offset-checkpoint

#### replication-offset-checpoint

#### meta.properties

#### .lock

### 日志格式

#### v0版本(0.10.0之前采用)

- offset

- message size

- Record
  - crc32
  - magic
    - 值为0
  - attributes
    - 1字节，低3位表示压缩类型：0-NONE，1-GZIP，2-SNAPPY，3-LZ4
  - key length
  - key
  - value length
  - value

#### v1版本(0.10.0 - 0.11.0版本之间采用)

- offset

- message size

- Record
  - crc32
  - magic
    - 值为0
  - timestamp
    - 时间戳
  - attributes
    - 1字节
    - 低3位表示压缩类型：0-NONE，1-GZIP，2-SNAPPY，3-LZ4
    - 第4位:时间戳类型，0-CreateTime，1-LogAppendTIme
  - key length
  - key
  - value length
  - value

#### v2版本(0.11.0版本开始使用)

- 变长字段
  - 每个字节都有一个位于最高为的msb位，处最后已一个字节外，其余的msb位都设置为1，最后一个字节的msb位为0，剩余的7位存储数据本身

- RecordBatch
  - first offset
    - 当前RecordBatch的起始位移
  - length
    - 从partition leader epoch字段开始到末尾的长度
  - partition leader epoch
    - 分区leader纪元
  - magic
    - 等于2
  - crc32
  - attributes
    - 消息属性2字节
      - 低3位压缩格式
      - 第4位时间戳类型
      - 第5位表示次RecordBatch是否处理事务中
        - 0-非事务
        - 1-事务
      - 第6位表示是否是控制消息
        - 0-非控制消息
        - 1-控制消息
  - last offset delta
    - RecordBatch中最后一个Record的offset与first offset的差值
  - first timestamp
    - 第一条Record的时间戳
  - max timestamp
    - 最大的时间戳
  - producer id
    - PID，用于支持事务和幂等
  - producer epoch
    - 用于支持事务和幂等
  - first sequence
    - 用于支持事务和幂等
  - records count
    - RecordBatch中Record的个数
  - records
    - length
      - 消息总长度
    - attributes
      - 弃用
    - timestamp delta
      - 时间戳增量
    - offset delta
      - 位移增量
    - key length
    - key
    - value lenght
    - value
    - headers count
    - headers
      - header key length
      - header key
      - header value length
      - header value

### 消息压缩

#### 配置key

- compression.type
  - producer
  - gzip
  - snappy
  - lz4
  - uncpmpressed

#### 外层消息(wrapper message)

- key为null

#### 内层消息(inner message)

- offset从0开始分配

### 日志索引

#### 索引文件

- 偏移量索引文件用来建立消息偏移量到物理地址之间的映射关系，方便快速定位消息所在的物理文件位置

- 时间戳文件则根据指定的时间戳，来查找对应的偏移量信息

#### 稀疏索引

- 并不能保证每个消息在索引文件中都有对应的索引项

- 每(log.index.interval.bytes:默认4KB)增加一个偏移量索引

#### 日志文件切分条件

- 分段日志大小超过log.segment.bytes配置的值

- 日志分段中的最大时间戳与当前系统时间戳的差值大于log.roll.ms或log.roll.hours配置的值

- 偏移量索引文件或时间戳索引文件大小达到了log.index.size.max.bytes配置的值

- 追加的消息的偏移量与当前日志分段的偏移量之间的差值大于Integer.MAX_VALUE

#### 偏移量索引

- 每个索引项8个字节
  - relativeOffset(相对偏移量)4B
  - position(物理位置)4B

#### 时间戳索引

- 每个索引项12个字节
  - timestamp(当前日志分段最大的时间戳)8B
  - relativeOffset(相对偏移量)4B

#### 索引查找

- 通过MappedByteBuffer将索引文件映射到内存中

- 2分查找

### 日志清理

#### 配置

- log.cleaner.enable(是否允许清理)

- log.clear.policy(清理策略)
  - delete
  - compact

- log.retention.check.interval.ms
  - 清理周期，默认5分钟

#### 策略

- 日志压缩
  - 定义
    - 针对每个消息的key进行整合，对于有相同key的不同value，只保留最后一个版本
  - 策略
    - 基于时间
      - 配置
        - log.retention
          - ms
          - minutes
          - hours
      - 判定策略
        - 根据日志分段的最大牌时间戳largestTimeStamp来计算，优先使用时间戳索引文件的最后一条索引项，其次才使用最近修改时间lastModifiedTime
      - 删除过程
        - 从Log对象中所维护的日志分段的跳跃表中移除待删除的日志分段，以保证没有线程对这些日志分段进行读取操作
        - 日志分段所对应的所有文件加上".deleted"的后缀
        - 提交一个以“delete-file”命名的延迟任务来删除这些以“.deleted”为后缀的文件
          - 删除任务的执行周期通过file.delete.delay.ms参数来配置，默认1分钟
    - 基于日志大小
      - 配置
        - log.retention.bytes
          - 所有日志文件的大小，-1表示无穷大
    - 基于日志起始偏移量

- 日志删除
  - 定义
    - 按照一定的保留策略直接删除不符合条件的日志分段
  - cleaner-offset-checkpoint
  - activeSegment不会参与其中
  - 每个消息的key不能为null
  - 过程
    - 第一次遍历把每个key的哈希值和最后出现offset都保留在SkimpyOffsetMap中
    - 第二次遍历会检查每个消息是否符合保留条件，符合则保留，否则被清理
  - 日志文件合并

### 磁盘存储

#### 文件追加的方式来写入消息

#### 页缓存

#### 磁盘IO流程(灵活选择调度策略)

- NOOP

- CFQ

- DEADLINE

- ANTICIPATORY

#### 零拷贝(DMA技术)

- Direct Memory Access
