# 消息发送

> 专题：RocketMQ
> 来源：RocketMQ.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `消息发送` 是 `RocketMQ` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `消息发送` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `消息发送`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `消息发送` 展开，属于 `RocketMQ` 的复习范围。
- 建议优先掌握：发送普通消息方式, topic路由机制, 消息发送高可用, RocketMQ消息(Message)。
- 二级节点提示了主要拆分角度：可靠同步发送, 可靠异步发送, 单向发送, 每隔30秒从NameServer同步路由信息, 创建新主题时，优先同NamerServer查询，若未查询到, 默认使用轮询算法进行路由，可自定义，但会导致重试机制失效, 默认重试两次, 故障规避机制, topic, 消息标记(flag)。
- 三级节点通常对应具体细节、API、机制或易错点：sync, async, one way, 是否开启了自动创建主题机制, 重试时会尽量避开刚刚接受失败的Broker, tags, keys, waitStoreMsgOK, clientId, 验证消息。

### 复习追问

- `发送普通消息方式` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `topic路由机制` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `消息发送高可用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `RocketMQ消息(Message)` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 消息发送

### 发送普通消息方式

#### 可靠同步发送

- sync

#### 可靠异步发送

- async

#### 单向发送

- one way

### topic路由机制

#### 每隔30秒从NameServer同步路由信息

#### 创建新主题时，优先同NamerServer查询，若未查询到

- 是否开启了自动创建主题机制
  - 未开启，则报错
  - 开启，则查找默认路由信息为新主题创建对应的路由信息

### 消息发送高可用

#### 默认使用轮询算法进行路由，可自定义，但会导致重试机制失效

#### 默认重试两次

#### 故障规避机制

- 重试时会尽量避开刚刚接受失败的Broker

### RocketMQ消息(Message)

#### topic

#### 消息标记(flag)

#### 扩展属性(properties)

- tags
  - 消息tag，用于消息过滤

- keys
  - 消息索引键，用空格隔开，RocketMQ会根据这些key快速检索消息

- waitStoreMsgOK
  - 消息发送时是否等消息存储完成后再返回

#### 消息体(body)

#### 生产者启动

- clientId
  - IP+instance+unitname
    - instance默认为DEFAULT，即进程id
    - unitname为可选项

#### 消息发送基本流程

- 验证消息
  - 消息长度验证
    - 不能0，且不能超过最大限制(默认4MB)

- 查找路由
  - 如果生产者中缓存了topic的路由信息，且该路由信息包含消息队列，则直接返回该路由信息
  - 如果没有缓存则向NameServer查询该topic的路由信息。
    - 没找到则抛异常

- 消息发送
  - 选择消息队列
    - sendWhichQueue自增再获取值，与消息路由中的队列个数取模
  - Broker故障延迟机制
    - sendLatencyFaultEnable=true
  - 消息发送
    - 检查消息发送是否合理
      - 检查Broker是否有写权限
      - 检查topic是否可以进行消息发送
      - 在NameServer端存储主题的配置信息
      - 检查队列
    - 如果消息重试次数超过允许的最大重试次数，则进入DLQ死信队列
      - 死信队列名为%DLQ%+消费组名
    - 消息存储
  - 批量消息发送
    - 处理方式
      - 单条消息内容使用固定格式进行存储
