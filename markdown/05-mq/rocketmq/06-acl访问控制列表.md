# ACL(访问控制列表)

> 专题：RocketMQ
> 来源：RocketMQ.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `ACL(访问控制列表)` 是 `RocketMQ` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `ACL(访问控制列表)` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `ACL(访问控制列表)`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `ACL(访问控制列表)` 展开，属于 `RocketMQ` 的复习范围。
- 建议优先掌握：如何使用ACL, 实现原理。
- 二级节点提示了主要拆分角度：Broker端开启ACL, 客户端使用ACL, Broker, 配置文件监听, 验证逻辑。
- 三级节点通常对应具体细节、API、机制或易错点：Broker端的配置文件中添加如下参数，开启ACL：aclEnable=true, 将RocketMQ中的distribution/conf/plain_acl.yml文件复制到${ROCKETMQ_HOME}/conf目录下, AclClientRPCHook, 判断aclEnable是否为true, 使用SPI机制加载配置的AccessValidator实现类, 遍历配置的访问验证器后，向Broker处理服务器注册钩子函数，在执行处理请求前被调用, 线程处理过程, 哪些配置支持动态生效, 使用全局白名单对客户端IP进行验证，只需白名单任意一个规则匹配即通过验证, Broker端ACL配置中有没有包含请求用户的访问控制规则。

### 复习追问

- `如何使用ACL` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `实现原理` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## ACL(访问控制列表)

### 如何使用ACL

#### Broker端开启ACL

- Broker端的配置文件中添加如下参数，开启ACL：aclEnable=true

- 将RocketMQ中的distribution/conf/plain_acl.yml文件复制到${ROCKETMQ_HOME}/conf目录下

#### 客户端使用ACL

- AclClientRPCHook

### 实现原理

#### Broker

- 判断aclEnable是否为true

- 使用SPI机制加载配置的AccessValidator实现类

- 遍历配置的访问验证器后，向Broker处理服务器注册钩子函数，在执行处理请求前被调用

#### 配置文件监听

- 线程处理过程
  - 每个500ms校验一次文件内容
  - 遍历该线程监听的文件
  - 计算当前配置文件的MD5值
  - 如果两次计算的md5值不同，意味着文件内容发生变化，调用对应的时间处理

- 哪些配置支持动态生效
  - ACL配置
  - TLS模式相关的密钥等配置

#### 验证逻辑

- 使用全局白名单对客户端IP进行验证，只需白名单任意一个规则匹配即通过验证

- Broker端ACL配置中有没有包含请求用户的访问控制规则

- 验证用户级别的IP白名单规则

- 验证签名是否一致

- 调用checkPerm检验资源的访问权限

- 验证当前操作是否需要管理员权限

- 操作的资源是否需要权限校验

- 遍历需要的权限和拥有的权限进行对比
