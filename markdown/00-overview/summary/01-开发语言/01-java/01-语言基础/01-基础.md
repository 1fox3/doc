# 基础

> 专题：重点汇总
> 来源：重点汇总.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 脑图内容

## 基础

### 常量

#### 分类

- 静态常量(类中)

- 成员常量(类中)

- 局部常量(类方法中)

### ==和equals

### equals和hashCode

### 统计代码耗时

#### StopWatch工具类

### s1 += 1

#### 会发生隐式强制类型转换，而s1 = s1 +1 则不会，如果s1的类型小于int则会报错

### 跳出多层循环

#### 在最外面的循环语句前定义一个标号，然后通过break 标号语句跳出嵌套循环

### final

#### 用法

- 修饰类

- 修饰方法

- 修饰变量

### main方法

#### 可被子类继承

#### 可以重载，但不能被重写

#### synchronized可以修饰main方法，达到同步效果

#### 去掉static编译成功，运行失败

### 语法糖

#### 字符串+拼接

#### 泛型与类型擦除

#### 自动装箱与拆箱

#### 变长参数

#### 增强for循环

#### 内部类

#### 枚举类

#### 断言

#### lambda表达式

#### try-with-resources

#### var类型推断

### 重写和重载

### Erro和Exception

#### Error

- OutOfMemoryError

#### Exception

- 受查异常

- 运行时异常(RuntimeException)

### 包装类

#### 自动装箱、拆箱

#### 包装类缓存

- valueOf才会用到

### switch支持的数据类型

#### 基本数据类型

#### 包装数据类型

#### 枚举类型

#### String

### 字符串

#### isEmpty和isBlank

#### String, StringBuilder,StringBuffer

- StringBuffer线程安全

#### 为什么不可变

- String类被final关键字修饰

- char[] 或byte[]也被final修饰

- 同时未暴露修改字符串的方法

#### intern()

- 字符串常量池

#### 长度限制

- 编译期间不能大于65535

- 运行期间，最大Integer.MAX_VALUE

#### +和contact

- contact只支持字符串，+可以支持字符串和基本类型

- +左右都可以为null，contact会报空指针

### 静态内部类和普通内部类区别

#### 静态内部类可以不依赖于外部类实例被实例化

#### 普通内部类需要外部类实例化后才能实例化

### 接口和抽象类区别

#### 一个是类，一个时接口

#### 接口没有构造方法，而抽象类有

#### 接口中的变量必须是public static final修饰的，是常量且不能被字节和外部修改

#### 类单继承，接口多继承

### 判断类或接口的派生关系

#### isAssignableFrom

### 创建对象方法

#### new

#### 克隆一个对象（Clonable接口）

#### DemoClass.class.newInstance()

#### clas.for("com.jd.DemoClass").newInstance()

#### DemoClass.class.getConstructor().newInstance()

#### 反序列化一个对象

### 反射

### 泛型

#### 分类 (48)

- 泛型类

- 泛型接口

- 泛型方法

#### 标识符

- T（Type-类型）

- R(Result-结果)

- K（Key-键）

- V（Value-值）

- E(Element-元素)

- N(Number-数字)

- ?(通配符，不确定类型)

### 深拷贝浅拷贝

#### 浅拷贝只会复制对象内部引用对象的引用地址

### JDK8提供的内置的函数式接口

#### Consumer<T>

- T最为输入，没有返回值

#### Supplier<T>

- 没有输入，返回T

#### Predicate<T>

- T作为输入，返回boolean

#### Function<T, R>

- T作为输入，R作为输出

#### BinaryOperator<T>

- 两个T作为输入，放回一个T作为输入

### Java日期格式中YYYY与yyyy的区别

#### y：年，当天所在的年份

#### 子主题 2

#### Y：Week year，当天所在周的年份，以周六所在的年份为准

### Stream

#### map和flatmap

- map适用于对每个元素进行简单的转换

- flatmap适用于对数组流进行平铺后合并

#### map、peek、foreach

- map用于对流中的每个元素进行映射处理，然后在形成新的流

- peek用于debug调试流中间结果，不能形成新的流，但能修改引用类型字段的值

- foreach用于遍历操作，会中断流操作

### @Repeatable
