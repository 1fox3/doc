# I/O通信模型

> 专题：Netty
> 来源：面试内容汇总/Netty.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `I/O通信模型` 是 `Netty` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `I/O通信模型` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `I/O通信模型`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `I/O通信模型` 展开，属于 `Netty` 的复习范围。
- 建议优先掌握：I/O模型, Java AIO。
- 二级节点提示了主要拆分角度：阻塞I/O模型, 非阻塞I/O模型, 多路复用I/O模型, 信号驱动I/O模型, 异步I/O模型, 服务端, 客户端, 用户处理器。
- 三级节点通常对应具体细节、API、机制或易错点：特点, 优点, 缺点, 典型, AsynchronousServerSocketChannel, AsynchronousSocketChannel, CompletionHandler。

### 复习追问

- `I/O模型` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Java AIO` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## I/O通信模型

### I/O模型

#### 阻塞I/O模型

- 特点
  - 在I/O执行的2个阶段都被阻塞

- 优点
  - 进程阻塞挂起不消耗CUP资源，及时响应每个操作
  - 实现难度低，开发应用较容易
  - 适合并发量小的网络应用开发

- 缺点
  - 不适合并发量大的应用，因为一个请求I/O会阻塞进程
  - 需要为每个请求分配一个处理进程以及响应，系统开销大

#### 非阻塞I/O模型

- 特点
  - 用户进程需要不断主动地询问内核数据准备好了没有

- 优点
  - 实现难度低，开发应用相对阻塞I/O较难

- 缺点
  - 进程轮询调用，消耗CPU的资源
  - 适合并发量较小切不需要及时响应的网络应用开发

#### 多路复用I/O模型

- 特点
  - 对于每一个socket，一般都设置成非阻塞，但是整个用户进程其实时一直被阻塞的，只不过进程时被select函数阻塞，而不是被Socket I/O阻塞

- 典型
  - Java NIO， Nginx(epoll, poll, select)

- 优点
  - 专一进程解决多个进程I/O的阻塞问题，性能好，Reactor模式
  - 适合高并发服务应用开发，一个进程/线程响应多个请求

- 缺点
  - 实现和开发应用难度较大

#### 信号驱动I/O模型

#### 异步I/O模型

- 特点
  - 真正的异步I/O

- 典型
  - Java7 AIO

- 优点
  - 不阻塞，数据一步到位，采用Proactor模式
  - 非常适合高性能，高并发应用

- 缺点
  - 需要操作系统底层支持，linux2.5内核首现，Linux2.6产品的内核标准特征
  - 实现和开发应用难度大

### Java AIO

#### 服务端

- AsynchronousServerSocketChannel

#### 客户端

- AsynchronousSocketChannel

#### 用户处理器

- CompletionHandler
