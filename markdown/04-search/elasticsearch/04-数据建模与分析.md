# 数据建模与分析

> 专题：Elasticsearch
> 来源：ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `数据建模与分析` 是 `Elasticsearch` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `数据建模与分析` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `数据建模与分析`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `数据建模与分析` 展开，属于 `Elasticsearch` 的复习范围。
- 建议优先掌握：文档构建方法, 用聚合做数据分析。
- 二级节点提示了主要拆分角度：扁平式结构, 数据反范式化, 嵌套对象, 父子关系, 5.0版本中默认启用分片请求缓存，配置”size“：0，这个对分析型用例时最有用的。用户只系统获取聚合结果，不必在结果中得到具体文档内容, 指标聚合-Metric集合, 桶聚合-Bucket聚合, 管道聚合-Pipeline聚合, 矩阵聚合-Matrix stats。
- 三级节点通常对应具体细节、API、机制或易错点：_parent, ES总是把有共同父亲文档的子文档都放在相同分片上，假如某个父文档的子文档特别多，分片就会不均衡，导致性能和存储空间问题, 通过parent_id查询所有子文档, 计算基础统计信息-min，max，sum, 一次性计算所有基础统计信息-stats信息, 计算扩展统计信息-extend_stats聚合, 计算唯一值计数-cardinality, terms, Parent, Sibling。

### 复习追问

- `文档构建方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `用聚合做数据分析` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 数据建模与分析

### 文档构建方法

#### 扁平式结构

#### 数据反范式化

#### 嵌套对象

#### 父子关系

- _parent
  - 父文档类型的名字

- ES总是把有共同父亲文档的子文档都放在相同分片上，假如某个父文档的子文档特别多，分片就会不均衡，导致性能和存储空间问题

- 通过parent_id查询所有子文档
  - parent_id
    - type
    - id

### 用聚合做数据分析

#### 5.0版本中默认启用分片请求缓存，配置”size“：0，这个对分析型用例时最有用的。用户只系统获取聚合结果，不必在结果中得到具体文档内容

#### 指标聚合-Metric集合

- 计算基础统计信息-min，max，sum

- 一次性计算所有基础统计信息-stats信息

- 计算扩展统计信息-extend_stats聚合

- 计算唯一值计数-cardinality

#### 桶聚合-Bucket聚合

- terms

#### 管道聚合-Pipeline聚合

- Parent
  - 父管道聚合
    - 派生聚合
    - 累积和聚合
    - 桶脚本聚合
    - 桶选择器聚合
    - 序列差分聚合

- Sibling
  - 兄弟管道聚合
    - avg、max、min、sum、stats桶聚合
    - 扩展统计桶聚合
    - 百分比桶聚合
    - 移动平均聚合

#### 矩阵聚合-Matrix stats

- matrixstats
  - count
    - 计算中用到的每个字段的示例数据个数
  - mean
    - 每个字段的平均值
  - variance
    - 对每个字段的度量值，表述示例数据离平均值的偏离有多少
  - skewness
    - 对每个字段的度量值，量化描述平均值周围的非对称分布
  - kurtosis
    - 对每个字段的度量值，描述分布的形状
  - covariance
    - 用矩阵量化地描述一个字段的改变怎样与另一个相关
  - correlation
    - 值为-1到1的转换矩阵，描述了字段分布之间的关系

- missing
  - 处理字段缺失时的默认值
