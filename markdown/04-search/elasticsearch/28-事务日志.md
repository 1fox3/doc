# 事务日志

> 专题：Elasticsearch
> 来源：面试内容汇总/ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `事务日志` 是 `Elasticsearch` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `事务日志` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `事务日志`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `事务日志` 展开，属于 `Elasticsearch` 的复习范围。
- 建议优先掌握：强制事务日志刷新, 相关配置, 数据恢复, 实时读取。
- 二级节点提示了主要拆分角度：_flush, index.translog.sync_interval, index.translog.durability, index.translog.flush_threshold_size, elasticsearch-translog truncate -d /path/to/trans/log/, pretty。
- 三级节点通常对应具体细节、API、机制或易错点：日志多久同步到磁盘上一次，默认5秒, 控制在每次索引、删除、更新或批量请求操作后，是否要同步并提交事务日志, 事务日志的最大容量，当容量超过该参数值时，就强行进行事务日志刷新操作，默认512MB。

### 复习追问

- `强制事务日志刷新` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `相关配置` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `数据恢复` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `实时读取` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 事务日志

### 强制事务日志刷新

#### _flush

### 相关配置

#### index.translog.sync_interval

- 日志多久同步到磁盘上一次，默认5秒

#### index.translog.durability

- 控制在每次索引、删除、更新或批量请求操作后，是否要同步并提交事务日志
  - request
    - 每次请求都会同步并提交
  - async
    - 每隔sync_interval才会在后台做一次同步和提交操作

#### index.translog.flush_threshold_size

- 事务日志的最大容量，当容量超过该参数值时，就强行进行事务日志刷新操作，默认512MB

### 数据恢复

#### elasticsearch-translog truncate -d /path/to/trans/log/

### 实时读取

#### pretty
