# 节点类型

> 专题：Elasticsearch
> 来源：面试内容汇总/ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `节点类型` 是 `Elasticsearch` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `节点类型` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `节点类型`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `节点类型` 展开，属于 `Elasticsearch` 的复习范围。
- 建议优先掌握：数据节点, 主节点, Ingest节点, 部落节点, 协调节点。
- 二级节点提示了主要拆分角度：负责保存数据，段合并，执行查询, node.data:true, 负责管理整个集群, node.master:true, 数据处理管道有一到多个ingest节点组成，有ingest节点负责每个环节的处理, node.ingest:true, 是一种特殊的协调节点，可以连接多个集群，并在所有集群中执行查询或其他操作, 查询的执行过程分为分散阶段和集中阶段，这2个阶段都由接受查询请求的协调节点来管理，它们也是集群中的负载均衡器。

### 复习追问

- `数据节点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `主节点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Ingest节点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `部落节点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `协调节点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 节点类型

### 数据节点

#### 负责保存数据，段合并，执行查询

#### node.data:true

### 主节点

#### 负责管理整个集群

#### node.master:true

### Ingest节点

#### 数据处理管道有一到多个ingest节点组成，有ingest节点负责每个环节的处理

#### node.ingest:true

### 部落节点

#### 是一种特殊的协调节点，可以连接多个集群，并在所有集群中执行查询或其他操作

### 协调节点

#### 查询的执行过程分为分散阶段和集中阶段，这2个阶段都由接受查询请求的协调节点来管理，它们也是集群中的负载均衡器
