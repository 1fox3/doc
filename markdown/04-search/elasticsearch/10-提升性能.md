# 提升性能

> 专题：Elasticsearch
> 来源：ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `提升性能` 是 `Elasticsearch` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `提升性能` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `提升性能`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `提升性能` 展开，属于 `Elasticsearch` 的复习范围。
- 建议优先掌握：查询验证, 获取详细查询执行报告的查询分析器, 热点线程, 集群扩展, 基于时间索引的管理。
- 二级节点提示了主要拆分角度：接口, profile, 结果分析, 参数, 自动创建副本, shrink API, rollover API。
- 三级节点通常对应具体细节、API、机制或易错点：_validate, 返回结果, profile.shard.id, profile.shard.searches, profile.shard.rewrite_time, profile.shard.collector, /_nodes/hot_threads, trheads, interval, type。

### 复习追问

- `查询验证` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `获取详细查询执行报告的查询分析器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `热点线程` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `集群扩展` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `基于时间索引的管理` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 提升性能

### 查询验证

#### 接口

- _validate
  - 验证查询，并不会真正执行

- 返回结果
  - valid
    - true,表明语句是正确的
  - _shards
    - validate API是随机在某个分片上执行的，因此总分片数总是1
  - explanations
    - 包含底层重写过的查询，如果查询正确，那么真正执行的就是这个语句，否则里面是对为什么查询不正确的详细解释

### 获取详细查询执行报告的查询分析器

#### profile

#### 结果分析

- profile.shard.id
  - 响应中包含的每个分片的唯一id

- profile.shard.searches
  - 包含查询执行详细信息的数组

- profile.shard.rewrite_time
  - 一次完整的查询改写过程所花费的总时间

- profile.shard.collector
  - 这一部分时关于运行查询的Lucene收集器内容

### 热点线程

#### 接口 (4)

- /_nodes/hot_threads

#### 参数

- trheads
  - 需要分析的线程数，默认3个

- interval
  - 为了计算线程在某项操作上花费时间的百分比，ES会对线程做二次检查，可以使用interval参数来定义2次检查的间隔时间，默认500ms

- type
  - 需要检查线程状态的类型，默认是cpu

- snapshots
  - 需要生成的堆栈的快照数量，默认是10个

### 集群扩展

#### 自动创建副本

- index.auto_expand_replicas:0-all

### 基于时间索引的管理

#### shrink API

- 减少索引数

- 过程
  - 创建一个与源索引有相同定义的目标索引，但主分片数更少
  - 创建硬连接，将源索引中的段连接到目标索引
  - 最后对目标索引执行恢复流程，类似索引关闭再打开

- 要求
  - 目标索引不能存在与集群中
  - 目标索引的分片数必须为源索引分片数的因子
  - 收缩操作节点必须有足够的磁盘空间

#### rollover API

- 更新索引
