# 数据转换与联盟搜索

> 专题：Elasticsearch
> 来源：面试内容汇总/ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `数据转换与联盟搜索` 是 `Elasticsearch` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `数据转换与联盟搜索` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `数据转换与联盟搜索`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `数据转换与联盟搜索` 展开，属于 `Elasticsearch` 的复习范围。
- 建议优先掌握：ingest管道, 联盟搜索。
- 二级节点提示了主要拆分角度：结构定义, 接口, 处理管道中的错误, 内置处理器，23种, 定义, 创建部落节点, 部落节点可以查询数据，但是不能通过部落节点创建索引, 处理索引冲突, 屏蔽写操作。
- 三级节点通常对应具体细节、API、机制或易错点：description, processors, _ingest, on_failure，出现错误时，处理过程, ignore_failure=true，忽略错误, Append处理器, Convert处理器, Grok处理器, 同是使用多个集群, 以tribe为前缀。

### 复习追问

- `ingest管道` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `联盟搜索` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 数据转换与联盟搜索

### ingest管道

#### 结构定义

- description
  - 描述

- processors
  - 处理器列表

#### 接口

- _ingest

#### 处理管道中的错误

- on_failure，出现错误时，处理过程

- ignore_failure=true，忽略错误

#### 内置处理器，23种

- Append处理器

- Convert处理器

- Grok处理器

### 联盟搜索

#### 定义

- 同是使用多个集群

#### 创建部落节点

- 以tribe为前缀

#### 部落节点可以查询数据，但是不能通过部落节点创建索引

#### 处理索引冲突

- tribe.on_conflict，默认any
  - any
    - 随机选择一个索引
  - drop
    - 忽略同名索引
  - prefer_TRIBE_NAME
    - 允许选择使用哪个集群的索引

#### 屏蔽写操作

- tribe.blocks.write
  - 所有写请求

- tribe.blocks.metadata
  - 禁止元数据修改

- tribe.blocks.indices.write
  - 禁止索引修改
