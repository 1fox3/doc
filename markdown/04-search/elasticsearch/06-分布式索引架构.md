# 分布式索引架构

> 专题：Elasticsearch
> 来源：ES.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 脑图内容

## 分布式索引架构

### 配置节点

#### cluser.name

#### network.host

#### discovery.zen.ping.unicast.hosts

### 分片和副本

#### 默认5个分片，1个副本

#### number_of_shards

#### number_of_replicas

### 路由

#### routing

- 给不同的文档使用相同的路由参数值，可以宝恒这些文档被索引到相同分片中

- 通过路由可以通过索引的一个特定的字节中获取数据

### 别名

#### alias

- index

- routing

- search_routing

- index_routing

### 分片分配控制

#### 配置

- node.attr.tag

- node.attr.group

- node.attr.rack_id

- node.attr.party

#### 部署策略

- cluster.routing.allocation.awareness.attributes
  - 可指定多个属性名，group,node

#### 强制部署意识

- cluster.routing.allocation.awareness.force.zone.values

#### 分片分配过滤

- 集群级别
  - cluster.routing.allocation.include
  - cluster.routing.allocation.require
  - cluster.routing.allocation.exclude

- 索引级别
  - index.routing.allocation.include
  - index.routing.allocation.require
  - index.routing.allocation.exclude

- 属性
  - _ip
  - _name
  - _host

- 更新
  - 集群：/_cluster/settings
  - 索引：/index_name/_settings

#### 每个节点允许的总分片数

- index.routing.allocation.total_shards_per_node

#### 主分片和副本是否可以部署在同一台物理机上

- cluster.routing.allocation.same_shard.host

#### 基于磁盘的分片分配控制

- cluster.routing.allocation.disk.watermark.low
  - 磁盘使用率高于这个值时，将不再分配新的分片。默认85%

- cluster.routing.allocation.disk.watermark.high
  - 磁盘使用率高于这个值时，将进行分片迁移，默认值为90%

- cluster.info.update.interval
  - 磁盘使用情况更新频率， 默认30秒一次

### 查询执行偏好

#### preference

- _primary

- _primary_first

- _replica

- _replica_first

- _local

- _prefer_node

- _shards

- custom

### 将数据切分到多个路径中

#### path.data:/data_path1/,/data_path2/
