# 循环依赖

> 专题：Spring
> 来源：面试内容汇总/Spring.xmind

## 补充与实践

- 补充：Spring 6 要求 Java 17+ 并迁移到 Jakarta 命名空间，理解 Bean 生命周期仍是排查问题的基础。
- 实践：复杂初始化逻辑优先使用明确的生命周期回调，避免在构造器里访问尚未注入完成的依赖。
- 误区：三级缓存只能解决部分单例 setter 循环依赖，构造器循环依赖和原型循环依赖无法靠它解决。

## 详细说明

### 是什么

- `循环依赖` 是 `Spring` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `循环依赖` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `循环依赖`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `循环依赖` 展开，属于 `Spring` 的复习范围。
- 建议优先掌握：三级缓存, 解决方式, spring.main.allow-circular-references=false。
- 二级节点提示了主要拆分角度：singletonObjects缓存, earlySingletonObject缓存, singletonFactories缓存, 当容器创建一个Bean时，会先将Bean加入到singletonFactories缓存中，并设置一个占位符, Spring会调用该Bean的构造函数或工厂方法创建实例，并对实例进行依赖注入, 如果在依赖注入的过程中发现该Bean依赖另外一个未初始化完成的Bean，Spring会先将该Bean的工厂实例(ObjectFactory)存储到earlySingletonObjects缓存中, Spring会继续初始化依赖的Bean，并将其添加到singletonObject缓存中, 如果依赖的Bean在初始化发现需要依赖当前Bean，那么会从earlySingletonObjects缓存中获取该Bean的ObjectFactory，并用它创建实例并注入当前Bean, 当该Bean初始化完成后，Spring会将其添加到singletonObjects缓存中，并将earlySingletonObject缓存和singletonFactories缓存中与该Bean相关的数据移除。
- 三级节点通常对应具体细节、API、机制或易错点：用于存储已经实例化的Bean, 用于存储提前暴露的Bean实例, 用于存储Bean工厂，即用于创建Bean的FactoryBean实例。

### 复习追问

- `三级缓存` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `解决方式` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `spring.main.allow-circular-references=false` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 循环依赖

### 三级缓存

#### singletonObjects缓存

- 用于存储已经实例化的Bean

#### earlySingletonObject缓存

- 用于存储提前暴露的Bean实例

#### singletonFactories缓存

- 用于存储Bean工厂，即用于创建Bean的FactoryBean实例

### 解决方式

#### 当容器创建一个Bean时，会先将Bean加入到singletonFactories缓存中，并设置一个占位符

#### Spring会调用该Bean的构造函数或工厂方法创建实例，并对实例进行依赖注入

#### 如果在依赖注入的过程中发现该Bean依赖另外一个未初始化完成的Bean，Spring会先将该Bean的工厂实例(ObjectFactory)存储到earlySingletonObjects缓存中

#### Spring会继续初始化依赖的Bean，并将其添加到singletonObject缓存中

#### 如果依赖的Bean在初始化发现需要依赖当前Bean，那么会从earlySingletonObjects缓存中获取该Bean的ObjectFactory，并用它创建实例并注入当前Bean

#### 当该Bean初始化完成后，Spring会将其添加到singletonObjects缓存中，并将earlySingletonObject缓存和singletonFactories缓存中与该Bean相关的数据移除

### spring.main.allow-circular-references=false
