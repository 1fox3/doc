# 事务

> 专题：Spring
> 来源：面试内容汇总/Spring.xmind

## 补充与实践

- 补充：Spring AOP 基于代理，事务、缓存、异步等注解都依赖代理边界。
- 实践：事务方法避免自调用，异常回滚规则要明确，跨服务事务优先采用最终一致性方案。
- 误区：`@Transactional` 默认只对运行时异常回滚，且 private/final 方法通常不会被代理增强。

## 详细说明

### 是什么

- `事务` 是 `Spring` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `事务` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `事务`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `事务` 展开，属于 `Spring` 的复习范围。
- 建议优先掌握：事务管理类型, 事务管理器接口, Spring AOP实现事务管理, Spring事务和数据库事务区别, 事务失效原因。
- 二级节点提示了主要拆分角度：编程式事务管理, 声明式事务管理, 注解驱动事务管理, PlatformTransactionManager（接口）, 使用代理, 实现机制不同, 控制方式不同, 使用场景不同, propagation, isolation。
- 三级节点通常对应具体细节、API、机制或易错点：通过编写代码来管理事务，包裹事务开启，回滚，确认等操作，需要手动控制事务的所有细节, 通过Spring AOP和事务拦截器来管理事务，无需手动控制事务，而是通过在配置文件中声明事务的属性来实现。声明式事务管理可以使用XML或注解配置, @Transactional, DataSourceTransactionManager, HibernateTransactionManager, JpaTransactionManager, JtaTransactionManager, ReactiveTransactionManager, Spring事务时应用层面上实现的，基于AOP和事务管理器等技术实现的事务控制和管理机制，它可以跨多个数据库操作，也可以和非数据库的操作一起工作，具有很大的灵活性和可扩展性, 数据库事务是由数据库本身提供的事务管理机制，是基于数据库引擎实现的，一个事务通常只包含对一个数据库的操作。

### 复习追问

- `事务管理类型` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `事务管理器接口` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Spring AOP实现事务管理` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Spring事务和数据库事务区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `事务失效原因` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 事务

### 事务管理类型

#### 编程式事务管理

- 通过编写代码来管理事务，包裹事务开启，回滚，确认等操作，需要手动控制事务的所有细节

#### 声明式事务管理

- 通过Spring AOP和事务拦截器来管理事务，无需手动控制事务，而是通过在配置文件中声明事务的属性来实现。声明式事务管理可以使用XML或注解配置

#### 注解驱动事务管理

- @Transactional

### 事务管理器接口

#### PlatformTransactionManager（接口）

- DataSourceTransactionManager
  - 适应于JDBC访问关系型数据库的场景

- HibernateTransactionManager
  - 适应于Hibernate访问关系型数据库的场景

- JpaTransactionManager
  - 适应于JPA访问关系型数据库的场景

- JtaTransactionManager
  - 适应于需要分布式事务支持的场景

- ReactiveTransactionManager
  - 适应于需要响应式事务的场景

### Spring AOP实现事务管理

#### 使用代理

### Spring事务和数据库事务区别

#### 实现机制不同

- Spring事务时应用层面上实现的，基于AOP和事务管理器等技术实现的事务控制和管理机制，它可以跨多个数据库操作，也可以和非数据库的操作一起工作，具有很大的灵活性和可扩展性

- 数据库事务是由数据库本身提供的事务管理机制，是基于数据库引擎实现的，一个事务通常只包含对一个数据库的操作

#### 控制方式不同

- Spring事务的隔离级别、超时、传播行为等都是有Spring框架提供

- 数据库事务的隔离级别和出阿比行为是由当前数据库本身决定的

#### 使用场景不同

- Spring事务可以与多种数据访问技术一起使用，如JDBC、Hibernate、JPA等，适用于各种复杂的业务场景

- 数据库事务只适应于对单个数据库的操作，并且一般使用在对数据完整性要求比较高的场景

### @Transactional

#### propagation

- 事务传播行为
  - REQUIRED（默认）
    - 如果当前存在事务，则加入该事务，否则新建一个事务
  - SUPPORTS
    - 如果当前存在事务，则加入该事务，否则以非事务方式执行
  - MANDATORY
    - 如果当前存在事务，则加入该事务，否则抛出异常
  - REQUIRES_NEW
    - 新建一个事务，并且暂停当前事务(如果存在)
  - NOT_SUPPORTED
    - 以非事务方式执行操作，并且暂停当前事务（如果存在）
  - NEVER
    - 以非事务方式执行，如果当前存在事务，则抛出异常
  - NESTED
    - 如果存在事务，则在嵌套事务中执行，否则新建一个事务

#### isolation

- 事务隔离级别
  - Default（默认）
    - 使用数据库默认隔离级别
  - READ_UNCOMMITTED
  - READ_COMMITTED
  - REPEATABLE_READ
  - SERIALIZABLE

#### timeout

- 事务的超时时间，默认-1，表示用数据库默认值

#### readOnly

- 是否为只读事务，默认false

#### rollbackFor

- 触发回滚的异常类型列表
  - 默认
    - RuntimeException
    - Error

#### noRollbackFor

- 不触发回滚的异常类型列表

### 事务失效原因

#### 数据库引擎不支持事务

#### 没有被Spring管理

#### 方法不是public，@Transactional只适用于public方法上

#### 自身调用问题

- 调用自身时，没有经过Spring代理类

#### 数据源没有配置事务管理器

#### 不支持事务

- 使用NOT_SUPPORTED传播行为

#### 异常被吃了

#### 异常类型错误
