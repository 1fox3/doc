# 常用注解

> 专题：Spring
> 来源：面试内容汇总/Spring.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `常用注解` 是 `Spring` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `常用注解` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `常用注解`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `常用注解` 展开，属于 `Spring` 的复习范围。
- 建议优先掌握：@Autowired, @Component, @Controller, @Service, @Repository, @Configuration, @Bean, @Value。
- 二级节点提示了主要拆分角度：自动装配Bean对象，可以用在字段，方法，构造函数上, 将类定义为Spring Bean，并有Spring托管, 声明类为Spring MVC控制器, 声明类为业务逻辑组件, 声明类数据库访问组件, 声明类为Spring配置类，用于定义Bean, 在Spring配置类中定义Bean, 注入属性值或外部配合文件中的值, 当存在多个相同的类型的Bean时，使用该注解指定要注入的Bean, 指定Bean的作用域。

### 复习追问

- `@Autowired` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Component` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Controller` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Service` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Repository` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 常用注解

### @Autowired

#### 自动装配Bean对象，可以用在字段，方法，构造函数上

### @Component

#### 将类定义为Spring Bean，并有Spring托管

### @Controller

#### 声明类为Spring MVC控制器

### @Service

#### 声明类为业务逻辑组件

### @Repository

#### 声明类数据库访问组件

### @Configuration

#### 声明类为Spring配置类，用于定义Bean

### @Bean

#### 在Spring配置类中定义Bean

### @Value

#### 注入属性值或外部配合文件中的值

### @Qualifier

#### 当存在多个相同的类型的Bean时，使用该注解指定要注入的Bean

### @Scope

#### 指定Bean的作用域

### @PostConstruct

#### 在Bean的初始化方法中执行

### @PreDestory

#### 在Bean的销毁方法中执行

### @RequestMapping

#### 映射URL到控制器方法

### @PathVariable

#### 将URL中的占位符映射到方法参数中

### @RequestParam

#### 将自定义的请求参数映射到方法参数中

### @ResponseBody

#### 在HTTP Body中输出数据

### @ExceptionHandler

#### 定义异常处理方法

### @Primary

#### 表示默认bean
