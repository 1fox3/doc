# JDBC核心对象

> 专题：MyBatis
> 来源：面试内容汇总/MyBatis.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `JDBC核心对象` 是 `MyBatis` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `JDBC核心对象` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `JDBC核心对象`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `JDBC核心对象` 展开，属于 `MyBatis` 的复习范围。
- 建议优先掌握：DriverManager, Connection, Statement, PreparedStatement, ResultSet。
- 二级节点提示了主要拆分角度：负责创建和管理JDBC驱动程序，可以使用getConnection()方法获取数据库连接, 数据库连接，提供一系列操作方法，例如：执行SQL，提交事务等, SQL语句核心对象，可以执行静态SQL, 预编译SQL语句，设置参数占位符，执行SQL并返回结果, 查询结果，提供了一些列方法，可遍历结果，并查询结果各列的值。

### 复习追问

- `DriverManager` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Connection` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Statement` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `PreparedStatement` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `ResultSet` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## JDBC核心对象

### DriverManager

#### 负责创建和管理JDBC驱动程序，可以使用getConnection()方法获取数据库连接

### Connection

#### 数据库连接，提供一系列操作方法，例如：执行SQL，提交事务等

### Statement

#### SQL语句核心对象，可以执行静态SQL

### PreparedStatement

#### 预编译SQL语句，设置参数占位符，执行SQL并返回结果

### ResultSet

#### 查询结果，提供了一些列方法，可遍历结果，并查询结果各列的值
