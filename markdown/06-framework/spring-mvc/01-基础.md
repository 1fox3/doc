# 基础

> 专题：Spring MVC
> 来源：面试内容汇总/Spring MVC.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `基础` 是 `Spring MVC` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `基础` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `基础`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `基础` 展开，属于 `Spring MVC` 的复习范围。
- 建议优先掌握：与Structs2的区别, HandlerMapping和HandlerAdatper区别, Spring MVC与RESTful API, Spring MVC如何获取当前request, @ControllerAdvice, Model和ModelAndView, Controller, 定义全局数据。
- 二级节点提示了主要拆分角度：Spring MVC时基于方法的拦截，即一个方法映射一个URL，是单例模式。而Structs2是基于类的拦截，每次请求就会创建一个Action，是多利模式, Spring MVC的入口时Servlet，即DispatcherServlet，而Structs2的入口时Filter，即StructsPrepareAndExecuteFilter, Spring MVC可以做到0配置文件，而Structs2则不行, Spring MVC开发效率、性能、安全性都要远高于Structs2, HandlerMapping负责确定并找到对应的控制器（Controller）处理当前请求，返回对应的控制器处理器对象（Handler）, HandlerAdapter负责使用控制器处理器对象（Handler）来处理请求，并返回ModelAndView对象, @PostMapping, @GetMapping, @DeleteMapping, 使用成员变量直接注入对象，不会有线程安全问题。
- 三级节点通常对应具体细节、API、机制或易错点：@Autowired, public String test(HttpServletRequest request), Model、ModelMap、Map都是BindingAwareModeMap的抽象, Model相对简单，只能添加参数, ModelMap实现了Map，所以它们2个可以进行增删改查等复杂操作，但ModelMap比Map功能更强大, Model->Map->ModelMap, 可以使用ThreadLocal, 改为多例模式@Scope("prototype"), name/value, required。

### 复习追问

- `与Structs2的区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `HandlerMapping和HandlerAdatper区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Spring MVC与RESTful API` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Spring MVC如何获取当前request` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@ControllerAdvice` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 基础

### 与Structs2的区别

#### Spring MVC时基于方法的拦截，即一个方法映射一个URL，是单例模式。而Structs2是基于类的拦截，每次请求就会创建一个Action，是多利模式

#### Spring MVC的入口时Servlet，即DispatcherServlet，而Structs2的入口时Filter，即StructsPrepareAndExecuteFilter

#### Spring MVC可以做到0配置文件，而Structs2则不行

#### Spring MVC开发效率、性能、安全性都要远高于Structs2

### HandlerMapping和HandlerAdatper区别

#### HandlerMapping负责确定并找到对应的控制器（Controller）处理当前请求，返回对应的控制器处理器对象（Handler）

#### HandlerAdapter负责使用控制器处理器对象（Handler）来处理请求，并返回ModelAndView对象

### Spring MVC与RESTful API

#### @PostMapping

#### @GetMapping

#### @DeleteMapping

### Spring MVC如何获取当前request

#### 使用成员变量直接注入对象，不会有线程安全问题

- @Autowired

#### 直接在方法定义request参数，可直接使用

- public String test(HttpServletRequest request)

### @ControllerAdvice

#### 可以通过组合使用@ExceptionHandler、@InitBinder和@ModelAttribute注解实现多种全局处理功能

### Model和ModelAndView

#### Model、ModelMap、Map仅用于在控制器和视图之间传递数据

- Model、ModelMap、Map都是BindingAwareModeMap的抽象

- Model相对简单，只能添加参数

- ModelMap实现了Map，所以它们2个可以进行增删改查等复杂操作，但ModelMap比Map功能更强大

- Model->Map->ModelMap

#### ModelAndView=Model+View，即除了传递模型数据之外，还可以支持视图跳转等设置，比如设置视图名称，错误信息等，让控制器能够更加灵活的控制请求的处理和响应

### Controller

#### 单例模式

#### 非线程安全

- 可以使用ThreadLocal

- 改为多例模式@Scope("prototype")

### 定义全局数据

#### @ModelAttribute

#### 可用于方法、方法参数上

### 可用于方法参数上的注解

#### @PathVariable

#### @RequestPart

#### @RequestParam

- name/value

- required

- defaultValue

#### @RequestHandler

#### @RequestBody

#### @RequestAtrribute

#### @ModelAttribute (25)

#### @SessionAttribute

#### @CookieValue

### 获取请求参数的方式

#### 直接从request对象中获取

#### 在方法上定义参数，参数名与请求参数一直

#### @RequestParam，@PathVariable

#### 方法上定义一个bean接收参数

#### @ResquestBody注解绑定参数，一般用于JSON与bean的绑定，@JsonProperty可用于处理参数名不一致的情况

### 怎么接收日期类型参数

#### @InitBinder

### 上传文件

#### @RequestPart注解绑定到MultipartFile文件类型的参数上

### 请求转发

#### forward：/new/path/to/load

#### 使用ModelAndView的构造函数指定新的路径

### 请求重定向

#### redirect

#### HttpServletResponse.sendRedirect

#### 使用ModelAndView的构造函数指定新的路径 (39)

### 只接收POST请求

#### @RequestMapping注解中指定method

#### @PostMapping (41)

### 限制请求数据格式

#### @RequestMapping中的consumes参数指定数据格式

### 指定响应数据格式

#### @RequestMapping中的produces参数指定数据格式

### 请求乱码问题

#### CharacterEncodingFilter

### Spring MVC接口返回类型

#### ModelAndView

#### String

#### Object

#### void

- forward和redirect

- @RestController情况下，即没有任何数据返回

- 非以上场景，会根据url直接解析视图，如果视图不存则抛异常

### 向前台页面传递数据

#### request.setAttribute

#### ModelAndView.addObject

#### Model，ModelMap，Map

### 参数解析器

#### RequestParamMethodArgumentResolver

#### PathVariableMethodArgumentResolver

### 消息转换器

#### ByteArrayHttpMessageConverter

#### StringHttpMessageConverter

#### ResourceHttpMessageConverter

#### MappingJackson2HttpMessageConverter

### 视图解析器

#### InternalResourceViewResolver

#### FreeMarkerViewResolver

#### ThymeleafViewResolver

#### ContentNegotiatingViewResolver

- 用于根据请求中的Accept头信息来选择合适的视图解析器，可以解析多种视图类型，如JSON，XML，HTML等

### 异常处理

#### @ControllerAdvice + @ExceptionHandler

#### 实现HandlerExceptionResolver接口，定义一个全局的异常处理器
