# 重点组件

> 专题：Spring MVC
> 来源：面试内容汇总/Spring MVC.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `重点组件` 是 `Spring MVC` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `重点组件` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `重点组件`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `重点组件` 展开，属于 `Spring MVC` 的复习范围。
- 建议优先掌握：DispatcherServlet, HandlerMapping, HandlerAdapter, Controller, ViewResolver, View, Model, HandlerInterceptor。
- 二级节点提示了主要拆分角度：Spring MVC的核心组件，用于接受HTTP请求并将请求分派到对应得控制器（Controller）进行处理, 根据请求URL和HTTP方法，将请求映射到响应的控制器处理器对象（Handler）, HandlerMapping是一个接口, 作用, 负责将请求交给控制器处理器对象（Handler）进行处理，并将处理结果封装为ModelAndView对象返回, HandlerAdapter是一个接口, Spring MVC会根据请求的URL和处理器（Handler）的类型选择合适的HandlerAdapter, 处理来自DispatcherServlet的请求，并返回相应得视图（View）, 用于将逻辑视图解析为具体的视图实现, 用于将模型数据渲染成HTML或其他格式的响应，以返回给客户端。
- 三级节点通常对应具体细节、API、机制或易错点：RequestMappingHandlerMapping, BeanNameUrlHandlerMapping, 请求URL到控制器（Controller）的映射注册, 根据请求URL找到对应的控制器类（Controller）, 拦截器的注册, RequestMappingHandlerAdapter, preHandle, postHandle, afterCompletion。

### 复习追问

- `DispatcherServlet` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `HandlerMapping` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `HandlerAdapter` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Controller` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `ViewResolver` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 重点组件

### DispatcherServlet

#### Spring MVC的核心组件，用于接受HTTP请求并将请求分派到对应得控制器（Controller）进行处理

### HandlerMapping

#### 根据请求URL和HTTP方法，将请求映射到响应的控制器处理器对象（Handler）

#### HandlerMapping是一个接口

- RequestMappingHandlerMapping

- BeanNameUrlHandlerMapping

#### 作用

- 请求URL到控制器（Controller）的映射注册

- 根据请求URL找到对应的控制器类（Controller）

- 拦截器的注册

### HandlerAdapter

#### 负责将请求交给控制器处理器对象（Handler）进行处理，并将处理结果封装为ModelAndView对象返回

#### HandlerAdapter是一个接口

- RequestMappingHandlerAdapter

#### Spring MVC会根据请求的URL和处理器（Handler）的类型选择合适的HandlerAdapter

### Controller

#### 处理来自DispatcherServlet的请求，并返回相应得视图（View）

### ViewResolver

#### 用于将逻辑视图解析为具体的视图实现

### View

#### 用于将模型数据渲染成HTML或其他格式的响应，以返回给客户端

### Model

#### 用于存储控制器（Controller）返回给视图（VIew）的数据

### HandlerInterceptor

#### 用于在请求被处理之后或之前，拦截和修改请求或响应

#### HandlerInterceptor是个接口

- preHandle
  - 请求到达控制器之前
  - 默认返回true

- postHandle
  - 请求到达控制器之后，视图渲染之前

- afterCompletion
  - 请求完成之后，视图渲染之后

#### 多个拦截器会组成一个拦截器链，默认情况下，按照配置的顺序来执行，也可自动以拦截器的执行顺序（@Order或Ordered接口）

### ExceptionHandler

#### 用于处理控制器（Controller）抛出的异常，以返回适当的错误响应
