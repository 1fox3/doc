# 常用注解

> 专题：Spring MVC
> 来源：面试内容汇总/Spring MVC.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `常用注解` 是 `Spring MVC` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `常用注解` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `常用注解`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `常用注解` 展开，属于 `Spring MVC` 的复习范围。
- 建议优先掌握：@Component, @Controller, @RestConroller, @Service, @Respository, @RequestMapping, @GetMapping, @PostMapping。
- 二级节点提示了主要拆分角度：用在类上，用于注册一个普通的Spring Bean, 用在类上，用于注册一个控制器, 用在类上，用于注册一个控制器，等于@Controller + @ResponseBody, 用在类上，用于注册一个服务层标识的Bean, 用在类上，同于注册一个DAO层标识的Bean, 用在类、方法上，映射URL的访问路径, 用在方法上，映射一个GET请求的URL, 用在方法上，映射一个POST请求的URL, 用在类，方法上，方法以http body形式响应数据, 用在方法参数上，从URL路劲上绑定参数。

### 复习追问

- `@Component` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Controller` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@RestConroller` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Service` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `@Respository` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 常用注解

### @Component

#### 用在类上，用于注册一个普通的Spring Bean

### @Controller

#### 用在类上，用于注册一个控制器

### @RestConroller

#### 用在类上，用于注册一个控制器，等于@Controller + @ResponseBody

### @Service

#### 用在类上，用于注册一个服务层标识的Bean

### @Respository

#### 用在类上，同于注册一个DAO层标识的Bean

### @RequestMapping

#### 用在类、方法上，映射URL的访问路径

### @GetMapping

#### 用在方法上，映射一个GET请求的URL

### @PostMapping

#### 用在方法上，映射一个POST请求的URL

### @ResponseBody

#### 用在类，方法上，方法以http body形式响应数据

### @PathVariable

#### 用在方法参数上，从URL路劲上绑定参数

### @RequestParam

#### 用在方法参数上，接收指定名称的请求参数

### @RequestBody

#### 用在方法参数上，表示从http body接受参数

### @SessionAttributes

#### 用在控制器类上，将Model中数据设置到Session中

### @SessionAttribute

#### 用在控制器方法参数上，设置方法参数的值从Session中获取
