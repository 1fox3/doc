# 基本信息

> 专题：Spring Boot
> 来源：面试内容汇总/SpringBoot.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 脑图内容

## 基本信息

### 核心思想

#### 约定大于配置

- 约定一些推荐的默认配置

- 研发只需要规定不符合约定的部分

### 核心配置文件

#### bootstrap

- 由父ApplicationContext加载，比application优先加载

- 属性不能被覆盖

- 主要用途
  - 一些固定不能被覆盖的属性
  - 一些加密、解密的场景

#### application

### 配置文件格式

#### properties

#### yml

- 不支持@PropertySource注解倒入配置

### 核心注解

#### @SpringBootApplication

- @SpringBootConfiguration
  - 组合了@Configuration注解，实现配置文件的功能

- @EnableAutoConfiguration
  - 打开自动配置功能，也可关闭某个自动配置的选项

- @ComponentScan
  - Spring扫描组件

### 集成方式

#### 继承spring-boot-starter-parent项目

- 属性覆盖仅对继承有效

#### 导入spring-boot-dependencies项目依赖

### 打包方式（packaging参数）

#### jar（默认）

#### war

### https支持

#### server.ssl.*参数配置

### 可以同时支持http和https

#### https通过配置支持

#### 子主题 2

#### http使用TomcatServletWebServiceFactory.addAdditionalTomcatConnectors支持

### 循环依赖

#### spring.main.allowCircularReference

### 配置加载顺序

#### properties文件

#### yml文件

#### 系统环境变量

#### 命令行参数

### 如何兼容老Spring项目

#### @ImportResource注解导入老Spring项目配置文件

### 注册事件监听器

#### META-INF/spring.factories文件增加ApplicationListener=自定义Listener

#### @Component注解

- 仅能监听上下文创建之后的事件

### 跨域问题

#### @CrossOrigin注解

#### 基于Filter

#### 修改全局配置

### Spring Boot 2.x主要变更

#### 配置变更

#### JDK版本升级

#### HTTP2支持

#### 配置属性绑定

#### 第三方类库升级

#### 响应式Spring编程支持

### 启动图案

#### banner配置

- banner.location
  - 配置图案路径

### 关闭图案

#### new SpringApplicationBuilder(Application.class).bannerMode(Banner.Mode.OFF).run(args)

### 编码

#### ut8（默认）

#### 修改

- server.servlet.encoding.charset=GBK

- server.servlet.encoding.force=true

### Failure Analyzers

#### 分析启动失败的情况

#### FailureAnalyzers接口

- AbstractFailureAnalyzer（抽象类）
  - 重写anaylze方法，并返回FailureAnalysis对象
  - 并在spring.factories中注册

### 优雅关闭

#### server.shutdown=graceful

#### spring.lifecycle.timeout-per-shutdown-phase:10s

### mvnw

#### 可以快速将Mavern集成到项目中

### 版本升级时的配置迁移

#### spring-boot-properties-migrator

- 可以分析应用程序的环境并在启动时打印诊断信息，并可以在运行时进行临时的配置迁移
