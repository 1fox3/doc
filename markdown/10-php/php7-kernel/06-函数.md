# 函数

> 专题：PHP7 内核剖析
> 来源：PHP7内核剖析.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `函数` 是 `PHP7 内核剖析` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `函数` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `函数`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `函数` 展开，属于 `PHP7 内核剖析` 的复习范围。
- 建议优先掌握：用户自定义函数, 内部函数, 函数调用。
- 二级节点提示了主要拆分角度：函数通过zend_function表示，而不是zend_op_array，因为php还有一种特殊的函数：内部函数, zend_function, 函数编译完成后，会插入EG（function_table）符号表中，使用函数时根据函数名称在该符号表中缩影zend_function，从而获得编译后的函数指令，这也意味着函数的作用域时全局的, 词法解析, 抽象语法树的编译, 不需要经历opline的编译过程，所以效率高于php自定义函数, 也会插入EG(function_table)符号表中, zend_internal_function, 函数调用被编译为ZEND_AST_CALL节点，该节点有2个子节点，分别用于函数名称即参数列表, zend_compile_call()。
- 三级节点通常对应具体细节、API、机制或易错点：type, common, zend_op_array op_array, zend_internal_function internal_function, returns_ref, T_STRING, parameter_list, return_type, inner_statement_list, 编译生成函数声明指令。

### 复习追问

- `用户自定义函数` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `内部函数` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `函数调用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 函数

### 用户自定义函数

#### 函数通过zend_function表示，而不是zend_op_array，因为php还有一种特殊的函数：内部函数

#### zend_function

- type
  - 函数类型

- common
  - 函数的名称，参数等信息

- zend_op_array op_array
  - 用户自定义函数

- zend_internal_function internal_function
  - 内部函数

#### 函数编译完成后，会插入EG（function_table）符号表中，使用函数时根据函数名称在该符号表中缩影zend_function，从而获得编译后的函数指令，这也意味着函数的作用域时全局的

#### 词法解析

- returns_ref
  - 是否返回引用

- T_STRING
  - 函数名

- parameter_list
  - 参数列表

- return_type
  - 返回值类型

- inner_statement_list
  - 函数体

#### 抽象语法树的编译

- 编译生成函数声明指令

- 参数及返回值类型的编译
  - zend_arg_info

- 函数体的编译

- 注册函数

### 内部函数

#### 不需要经历opline的编译过程，所以效率高于php自定义函数

#### 也会插入EG(function_table)符号表中

#### zend_internal_function

### 函数调用

#### 函数调用被编译为ZEND_AST_CALL节点，该节点有2个子节点，分别用于函数名称即参数列表

#### zend_compile_call()

- 函数初始化指令
  - 根据函数名从EG(function_table)找到zend_function,然后分配被调函数的zend_execute_data内存
  - 函数在定义后调用
    - ZEND_INIT_FCALL
  - 函数在定义前调用
    - ZEND_INIT_FCALL_BY_NAME

- 参数发送指令
  - 根据实际传参，将参数的value传递递给被调参数内接受参数的变量，也就是zend_execute_data上的参数，每个参数都会生成这样一条指令。

- 函数执行指令
  - 根据调用函数不同执行不同的指令
    - ZEND_DO_ICALL
      - 内部函数
    - ZEND_DO_UCALL
      - 调用已定义了的用户自定义函数
    - ZEND_DO_FCALL_BY_NAME
      - 调用未定义的函数
    - ZEND_DO_FCALL
      - 自定义执行器的情况
