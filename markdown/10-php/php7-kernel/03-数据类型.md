# 数据类型

> 专题：PHP7 内核剖析
> 来源：PHP7内核剖析.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `数据类型` 是 `PHP7 内核剖析` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `数据类型` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `数据类型`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `数据类型` 展开，属于 `PHP7 内核剖析` 的复习范围。
- 建议优先掌握：zval, 字符串, 数组, 引用, 类型转换。
- 二级节点提示了主要拆分角度：_zcal_struct, _zend_string, 散列表, _zend_array, 映射表, 扩容, _zend_refrernce, PHP不存在多级指针的概念，引用只有一级, 对象转换为数组。
- 三级节点通常对应具体细节、API、机制或易错点：zend_value, u1, u2, zend_refcounted_h gc, zend_ulong h, size_t len, char val[1], 根据关键码值直接进行访问的数据结构，它的key-value之间存在一个映射函数，可以根据key通过映射函数直接所引导对应的value值, union u, unit32_t nTableMask。

### 复习追问

- `zval` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `字符串` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `数组` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `引用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `类型转换` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 数据类型

### zval

#### _zcal_struct

- zend_value
  - 定义了众多类型的指针

- u1
  - 数据类型
  - 类型掩码

- u2
  - 辅助功能

### 字符串

#### _zend_string

- zend_refcounted_h gc
  - 变量的引用计数信息，用于内存管理

- zend_ulong h
  - 字符串通过Times33算法计算的到的HashCode

- size_t len
  - 字符串长度

- char val[1]
  - 字符串内容

### 数组

#### 散列表

- 根据关键码值直接进行访问的数据结构，它的key-value之间存在一个映射函数，可以根据key通过映射函数直接所引导对应的value值

#### _zend_array

- zend_refcounted_h gc
  - 变量的引用计数信息，用于内存管理

- union u
  - 提供一些辅助作用

- unit32_t nTableMask
  - 用于散列函数映射存储元素在arData数组中的下标

- Bucket *arData
  - 存储元素数组，每个元素结构统一为Bucket，arData指向第一个Bucket

- unit32_t nNumUsed
  - 已用Bucket数，数据不会马上删除，只是标记为IS_UNDEF，当需要扩容时才会删除

- unit32_t nNumOfElements
  - 数组实际存储的元素数

- unit32_t nTableSize
  - 数组总容量

- unit32_t nInternalPointer

- zend_long nNextFreeElement
  - 下一个可用的数值索引

- dtor_func_t pDescructor
  - 删除或覆盖数组中某个值时，会调用此函数

#### 映射表

- 用于保存实际存储的有序数组中的下标与hash坐标之间的映射关系

- 与arData放在一起

- 通过链表的形式处理Hash冲突，头插法

#### 扩容

- 检查数组中已删除的元素所占的比例
  - 达到阈值则触发重建索引的操作，并移除已删除的Bucket
  - 未达到阈值，则扩大2倍，后复制原数组中的数据到新数组上，最后重建索引

### 引用

#### _zend_refrernce

- zend_refcounted_h gc
  - 变量的引用计数信息，用于内存管理

- zval val
  - 指向原来的value

#### PHP不存在多级指针的概念，引用只有一级

### 类型转换

#### 对象转换为数组

- private
  - 类名+属性名

- protected
  - “*”前缀+属性名

- public
  - 直接使用属性名
