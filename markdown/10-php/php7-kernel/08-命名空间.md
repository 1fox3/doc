# 命名空间

> 专题：PHP7 内核剖析
> 来源：PHP7内核剖析.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `命名空间` 是 `PHP7 内核剖析` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `命名空间` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `命名空间`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `命名空间` 展开，属于 `PHP7 内核剖析` 的复习范围。
- 建议优先掌握：命名空间作用, PHP命名空间只能隔离类、函数、常量和接口，不包括全局变量, namespace关键字声明命名空间，除了declare之外任何代码不能出现在namespace之前, 命名空间没有文件限制，可以在多个文件中声明同一个命名空间，也可以在一个文件中声明多个命名空间, ZEND_AST_NAMESAPCE, 编译过程中吧FC(current_namespace)设置为当前的命名空间，FC为CG(file_context),file_context时编译过程中的使用的一个辅助接口，在编译抽象语法树之前分配, use。
- 二级节点提示了主要拆分角度：处理用户编写的代码与PHP内部的定义或第三方定义的名字冲突, 为很长的标识符名称创建一个别名，提高源代码的可读性, 编译期间如果发现use语句，那么就把这个use后的命名空间插入一个哈希表FC（imports），而哈希表的key就是定义的别名，如果没有定义别名，则key默认使用按“、”分割的最后一节, 使用类、函数和常量时会进行自动补全，把名称按“\”分割，延后以第一节为key查找FC（imports）。

### 复习追问

- `命名空间作用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `PHP命名空间只能隔离类、函数、常量和接口，不包括全局变量` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `namespace关键字声明命名空间，除了declare之外任何代码不能出现在namespace之前` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `命名空间没有文件限制，可以在多个文件中声明同一个命名空间，也可以在一个文件中声明多个命名空间` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `ZEND_AST_NAMESAPCE` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 命名空间

### 命名空间作用

#### 处理用户编写的代码与PHP内部的定义或第三方定义的名字冲突

#### 为很长的标识符名称创建一个别名，提高源代码的可读性

### PHP命名空间只能隔离类、函数、常量和接口，不包括全局变量

### namespace关键字声明命名空间，除了declare之外任何代码不能出现在namespace之前

### 命名空间没有文件限制，可以在多个文件中声明同一个命名空间，也可以在一个文件中声明多个命名空间

### ZEND_AST_NAMESAPCE

### 编译过程中吧FC(current_namespace)设置为当前的命名空间，FC为CG(file_context),file_context时编译过程中的使用的一个辅助接口，在编译抽象语法树之前分配

### use

#### 编译期间如果发现use语句，那么就把这个use后的命名空间插入一个哈希表FC（imports），而哈希表的key就是定义的别名，如果没有定义别名，则key默认使用按“、”分割的最后一节

#### 使用类、函数和常量时会进行自动补全，把名称按“\”分割，延后以第一节为key查找FC（imports）
