# 处理状态变化

> 专题：ZooKeeper
> 来源：ZooKeeper.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `处理状态变化` 是 `ZooKeeper` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `处理状态变化` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `处理状态变化`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `处理状态变化` 展开，属于 `ZooKeeper` 的复习范围。
- 建议优先掌握：监视点, Multiop, Transaction, 顺序保障。
- 二级节点提示了主要拆分角度：客户端设置的每个监视点与会话关联，如果会话过期，等待中的监视点将会被删除, 当一个客户端与一个服务端连接断开后连接到一个新的服务端，客户端会发送未触发的监视点列表，在注册服务点时，服务端要检查已监视的znode节点在之前注册监视点之后是否已发生变化，如果znode节点已发生变化，一个监视点的时间就会被发送给客户端，否则在新的服务端上注册监视点, 创建ZooKeeper对象是，可传递一个默认的Watcher对象, 类型, 监视点删除, 3.4.0版本新加入的特性, 可以原子地执行多个ZooKeeper操作，操作要不全部成功，要不全部失败, 支持异步回调, multi方法, 检查znode节点版本号。
- 三级节点通常对应具体细节、API、机制或易错点：数据监视点, 子节点监视点, 触发这个监视点, 会话关闭或过期, check。

### 复习追问

- `监视点` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Multiop` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Transaction` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `顺序保障` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 处理状态变化

### 监视点

#### 客户端设置的每个监视点与会话关联，如果会话过期，等待中的监视点将会被删除

#### 当一个客户端与一个服务端连接断开后连接到一个新的服务端，客户端会发送未触发的监视点列表，在注册服务点时，服务端要检查已监视的znode节点在之前注册监视点之后是否已发生变化，如果znode节点已发生变化，一个监视点的时间就会被发送给客户端，否则在新的服务端上注册监视点

#### 创建ZooKeeper对象是，可传递一个默认的Watcher对象

#### 类型

- 数据监视点
  - exists
  - getData

- 子节点监视点
  - getChildren

#### 监视点删除

- 触发这个监视点

- 会话关闭或过期

### Multiop

#### 3.4.0版本新加入的特性

#### 可以原子地执行多个ZooKeeper操作，操作要不全部成功，要不全部失败

#### 支持异步回调

#### multi方法

#### 检查znode节点版本号

- check

### Transaction

#### 封装了multi方法

#### 支持异步回调 (12)

### 顺序保障

#### 写操作顺序

#### 读操作顺序

#### 通知顺序
