# 处理状态变化

> 专题：ZooKeeper
> 来源：ZooKeeper.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 脑图内容

## 处理状态变化

### 监视点

#### 客户端设置的每个监视点与会话关联，如果会话过期，等待中的监视点将会被删除

#### 当一个客户端与一个服务端连接断开后连接到一个新的服务端，客户端会发送未触发的监视点列表，在注册服务点时，服务端要检查已监视的znode节点在之前注册监视点之后是否已发生变化，如果znode节点已发生变化，一个监视点的时间就会被发送给客户端，否则在新的服务端上注册监视点

#### 创建ZooKeeper对象是，可传递一个默认的Watcher对象

#### 类型

- 数据监视点
  - exists
  - getData

- 子节点监视点
  - getChildren

#### 监视点删除

- 触发这个监视点

- 会话关闭或过期

### Multiop

#### 3.4.0版本新加入的特性

#### 可以原子地执行多个ZooKeeper操作，操作要不全部成功，要不全部失败

#### 支持异步回调

#### multi方法

#### 检查znode节点版本号

- check

### Transaction

#### 封装了multi方法

#### 支持异步回调 (12)

### 顺序保障

#### 写操作顺序

#### 读操作顺序

#### 通知顺序
