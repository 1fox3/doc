# 线程池

> 专题：Dubbo
> 来源：面试内容汇总/Dubbo.xmind

## 补充与实践

- 补充：Dubbo 3 支持 Triple 协议和应用级服务发现，适合云原生与多语言互通场景。
- 实践：接口超时应小于调用方整体超时预算，重试只适合幂等读或可重复操作。
- 排障：RPC 慢调用要同时看客户端等待、网络耗时、序列化、服务端线程池和业务执行。

## 详细说明

### 是什么

- `线程池` 是 `Dubbo` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `线程池` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `线程池`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `线程池` 展开，属于 `Dubbo` 的复习范围。
- 建议优先掌握：分类, 线程池确定时机, 自定义线程池。
- 二级节点提示了主要拆分角度：FixedThreadPool(默认), LimitedThreadPool, EagerThreadPool, CachedThreadPool, 确定线程模型时通过ChannelHandler指定线程池模型, 实现扩展接口ThreadPool, 增加接口配置（/META-INF/dubbo）, 服务提供者启动时指定线程池策略。
- 三级节点通常对应具体细节、API、机制或易错点：固定个数的线程池, 使用ThreadPoolExecutor创建, 线程池中的线程个数随着需求量动态增加，但不会超过配置的阈值，线程空闲也不会回收, 所有核心线程处于忙碌状态时，将创建新的线程来执行新任务，而不是将任务放入阻塞队列中, 与ThreadPoolExecutor不同, 自适应线程池，线程空闲1分钟时会被回收，有新请求到来时会创建新请求。

### 复习追问

- `分类` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `线程池确定时机` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `自定义线程池` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 线程池

### 分类

#### FixedThreadPool(默认)

- 固定个数的线程池

- 使用ThreadPoolExecutor创建

#### LimitedThreadPool

- 线程池中的线程个数随着需求量动态增加，但不会超过配置的阈值，线程空闲也不会回收

- 使用ThreadPoolExecutor创建

#### EagerThreadPool

- 所有核心线程处于忙碌状态时，将创建新的线程来执行新任务，而不是将任务放入阻塞队列中

- 与ThreadPoolExecutor不同
  - 核心线程数达到阈值后，会开启新线程处理任务，当线程数达到最大数时，才会把任务放入线程池队列

#### CachedThreadPool

- 自适应线程池，线程空闲1分钟时会被回收，有新请求到来时会创建新请求

- 使用ThreadPoolExecutor创建

### 线程池确定时机

#### 确定线程模型时通过ChannelHandler指定线程池模型

### 自定义线程池

#### 实现扩展接口ThreadPool

#### 增加接口配置（/META-INF/dubbo）

#### 服务提供者启动时指定线程池策略
