# 基础使用

> 专题：Dubbo
> 来源：Dubbo.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `基础使用` 是 `Dubbo` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `基础使用` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `基础使用`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `基础使用` 展开，属于 `Dubbo` 的复习范围。
- 建议优先掌握：服务发布与调用, 服务端异步执行, 使用AsyncContext异步执行, 服务消费端泛化调用, mock。
- 二级节点提示了主要拆分角度：发布, 同步调用, 异步调用, 子主题 4, CompletableFuture.supplyAsync(), RpcContext.startAsync(), asyncContext.signalContextSwitch(), 泛化接口调用方式主要在服务消费端没有API接口及模型类元(比如入参和出参的POJO类)的情况下使用，其参数及返回值中没有对应的POJO类，所以所有的POJO参数均转换为Map表示。使用泛化调用时，服务消费块不在需要引入SDK二访包, 根据序列化方式不同，分为如下方式, 消费端本地mock。
- 三级节点通常对应具体细节、API、机制或易错点：构建ServiceConfig实例, 指定注册中心地址, 设置接口与实现类, 设置服务分组和版本, 设置线程池策略, 导出服务, 挂起线程，避免服务停止, 创建ReferenceConfig实例, 设置应用程序信息, 设置服务注册中心地址。

### 复习追问

- `服务发布与调用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `服务端异步执行` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `使用AsyncContext异步执行` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `服务消费端泛化调用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `mock` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 基础使用

### 服务发布与调用

#### 发布

- 构建ServiceConfig实例

- 指定注册中心地址

- 设置接口与实现类

- 设置服务分组和版本

- 设置线程池策略

- 导出服务

- 挂起线程，避免服务停止

#### 同步调用

- 创建ReferenceConfig实例

- 设置应用程序信息

- 设置服务注册中心地址

- 设置服务接口和超时时间

- 设置自定义负载均衡策略与集群容错策略

- 设置服务分组与版本

- 引用服务

- 设置隐式参数

- 调用服务

#### 异步调用

- 调用ReferenceConfig实例的setAsync(true)

- 接口调用直接返回null

- Future
  - RpcContext.getContext().getFuture()获取future对象
  - future.setCallback设置回调函数ResponseCallBack
    - done
      - 正常返回响应结果
    - caught
      - 调用出现错误

- CompletableFuture
  - RpcContext.getContext().getCompletableFuture()获取Completablefuture对象
  - completableFuture.whenComplete

#### 子主题 4

### 服务端异步执行

#### CompletableFuture.supplyAsync()

### 使用AsyncContext异步执行

#### RpcContext.startAsync()

#### asyncContext.signalContextSwitch()

### 服务消费端泛化调用

#### 泛化接口调用方式主要在服务消费端没有API接口及模型类元(比如入参和出参的POJO类)的情况下使用，其参数及返回值中没有对应的POJO类，所以所有的POJO参数均转换为Map表示。使用泛化调用时，服务消费块不在需要引入SDK二访包

#### 根据序列化方式不同，分为如下方式

- true(generic=true)
  - 泛化参数固定为GenericService
    - ReferenceConfig<GenericService>
  - 设置泛化类型为true
    - referenceConfig.setGeneric(true)
  - 获取引用
    - 注意泛化值类型为GenericService

- bean(generic=bean)
  - JavaBeanSerializeUtil.serialize把参数序列化
  - JavaBeanSerializeUtil.deserialize把结果反序列化

- naticejava(generic=nativejava)
  - UnsafeByteArrayOutputStream
  - ExtensionLoader
  - 将参数序列化

### mock

#### 消费端本地mock

- 首先实现服务接口的mock实现类，类名必须为接口包名.类名MOCK的形式

- 开启mock
  - referenceConfig.setCheck(false)
  - referenceConfig.setMock(true)

- 执行mock服务实现类mock()方法之前，会发起远程调用，当全程调用失败(服务不存在时)，才会降级执行mock功能

#### 服务降级

- force
  - 直接返回客户端内设置的mock值

- fail
  - 仅当调用服务提供方失败后返回mock值，否者返回正常结果

### 隐式参数传递

#### RpcContext.getContext().setAttachment(key, val)

### 本地服务暴露与引用

#### 在同一个JVM进程中，同时发布与调用同一个服务

#### referenceConfig.setCheck(false)
