# 容错策略

> 专题：Dubbo
> 来源：面试内容汇总/Dubbo.xmind

## 补充与实践

- 补充：Dubbo 3 支持 Triple 协议和应用级服务发现，适合云原生与多语言互通场景。
- 实践：接口超时应小于调用方整体超时预算，重试只适合幂等读或可重复操作。
- 排障：RPC 慢调用要同时看客户端等待、网络耗时、序列化、服务端线程池和业务执行。

## 详细说明

### 是什么

- `容错策略` 是 `Dubbo` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `容错策略` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `容错策略`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `容错策略` 展开，属于 `Dubbo` 的复习范围。
- 建议优先掌握：Failover Cluster（默认）, Failfast Cluster, Failsafe Cluster, Failback Cluster, Forking Cluster, Broadcast Cluster, 自定义。
- 二级节点提示了主要拆分角度：失败重试,需配置重试次数, <dubbo:refreence retires="2"/>, <dubbo:method name=“sayHello” retires="2"/>, 快速失败, 仅调用一次, 安全失败, 忽略调用时的异常, 失败自动恢复, 调用失败后，在后台记录失败的请求，并按照一定的策略再进行重试, 并行调用。
- 三级节点通常对应具体细节、API、机制或易错点：针对具体方法。

### 复习追问

- `Failover Cluster（默认）` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Failfast Cluster` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Failsafe Cluster` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Failback Cluster` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Forking Cluster` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 容错策略

### Failover Cluster（默认）

#### 失败重试,需配置重试次数

#### <dubbo:refreence retires="2"/>

#### <dubbo:method name=“sayHello” retires="2"/>

- 针对具体方法

### Failfast Cluster

#### 快速失败

#### 仅调用一次

### Failsafe Cluster

#### 安全失败

#### 忽略调用时的异常

### Failback Cluster

#### 失败自动恢复

#### 调用失败后，在后台记录失败的请求，并按照一定的策略再进行重试

### Forking Cluster

#### 并行调用

#### 并行调用多个服务提供者，只要其中一个返回成功即可

#### forkes指定并行数

### Broadcast Cluster

#### 广播调用

#### 逐个调用所有的服务提供者，任意一台服务器调用异常则这次调用失败

### 自定义

#### 扩展Cluster接口
