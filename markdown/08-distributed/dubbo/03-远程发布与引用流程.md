# 远程发布与引用流程

> 专题：Dubbo
> 来源：Dubbo.xmind

## 补充与实践

- 补充：Dubbo 3 支持 Triple 协议和应用级服务发现，适合云原生与多语言互通场景。
- 实践：接口超时应小于调用方整体超时预算，重试只适合幂等读或可重复操作。
- 排障：RPC 慢调用要同时看客户端等待、网络耗时、序列化、服务端线程池和业务执行。

## 详细说明

### 是什么

- `远程发布与引用流程` 是 `Dubbo` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `远程发布与引用流程` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `远程发布与引用流程`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `远程发布与引用流程` 展开，属于 `Dubbo` 的复习范围。
- 建议优先掌握：服务发布启动流程, 服务处理请求, 消费启动流程, 消费调用。
- 二级节点提示了主要拆分角度：知识点, 流程。
- 三级节点通常对应具体细节、API、机制或易错点：默认同时支持本地导出和远程导出协议, 在dubbo中一个服务可以注册到多个服务中心, 延时发布, ServiceConfig属性进行合法性检查, 使用MetadataReportConfig设置了元数据存储信息, 服务端导出Invoker实例并舒转化为Wrapper类, RegistryProtocol的export方法启动NettyServer进行监听服务，并完成了Invoker到Exporter的转换, 通过RegistryFactory将服务注册到注册中心, NettyServer的connected方法会被激活, DubboProtocol的invoke方法创建Invocation。

### 复习追问

- `服务发布启动流程` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `服务处理请求` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `消费启动流程` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `消费调用` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 远程发布与引用流程

### 服务发布启动流程

#### 知识点

- 默认同时支持本地导出和远程导出协议
  - ServiceConfig的setScope设置
    - none（不导出服务）
    - remote（只导出远程服务）
    - local（只导出本地服务）

- 在dubbo中一个服务可以注册到多个服务中心

- 延时发布
  - ServiceConfig的setDelay方法设置延迟发布时间
  - ScheduledExecutorService为延时发布实现类

#### 流程

- ServiceConfig属性进行合法性检查

- 使用MetadataReportConfig设置了元数据存储信息

- 服务端导出Invoker实例并舒转化为Wrapper类

- RegistryProtocol的export方法启动NettyServer进行监听服务，并完成了Invoker到Exporter的转换

- 通过RegistryFactory将服务注册到注册中心
  - Zookeeper四层节点信息
    - Dubbo
    - 接口
    - type(Prividers)
    - 服务器ip:port地址

### 服务处理请求

#### 流程 (3)

- NettyServer的connected方法会被激活

- DubboProtocol的invoke方法创建Invocation

- Dubbo类的reply()方法执行具体的服务

### 消费启动流程

#### 流程 (4)

- 创建代理类
  - 本地引用
  - 指定服务提供方调用(直连方式)
  - 根据注册中心装配URL对象

- RefistryProtocol获取服务提供者列表

- DuboProtocol的refer方法获取Invoker

- 创建NettyClient对象

### 消费调用

#### 流程 (5)
