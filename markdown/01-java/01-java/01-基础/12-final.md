# final

## 用法

| 修饰目标 | 效果 |
|----------|------|
| 类 | 不能被继承，如 `String` |
| 方法 | 不能被子类重写 |
| 变量 | 只能赋值一次；引用类型不可重新指向其他对象，但对象内部状态仍可改变 |

## 注意点

- `final List` 不能重新赋值为另一个 List，但仍可调用 `add` 修改集合内容。
- `final` 字段在构造完成后具备安全发布语义，其他线程通常能看到正确初始化后的值。
- `final` 不等于线程安全，引用指向可变对象时，对象内部仍需同步或不可变设计。
