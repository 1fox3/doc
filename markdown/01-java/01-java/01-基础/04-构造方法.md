# 构造方法

## 核心规则

- 名称与类名相同，没有返回值，且不能声明为 `void`。
- 在创建对象时自动执行，无需手动调用。
- 未显式定义时，编译器自动生成默认无参构造；一旦定义了任意构造方法，默认无参构造不再自动生成。
- 子类构造方法默认先调用父类无参构造，可通过 `super(...)` 指定调用父类的其他构造方法。

## 注意点

- 框架通过反射创建对象（如 ORM、序列化工具）通常依赖无参构造，需确保其存在。
- 构造器中不要启动线程或暴露 `this`，避免对象未完成初始化就被外部访问。
