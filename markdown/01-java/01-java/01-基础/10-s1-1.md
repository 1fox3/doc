# s1 += 1

## 核心区别

| 写法 | 等价形式 | 编译结果 |
|------|----------|----------|
| `s1 += 1` | `s1 = (short)(s1 + 1)` | 隐含强制类型转换，编译通过 |
| `s1 = s1 + 1` | — | `s1 + 1` 提升为 `int`，赋值给 `short` 编译失败 |

## 原理

`byte`、`short`、`char` 参与算术运算时会自动提升为 `int`（Java 数值提升规则），复合赋值运算符（`+=`、`-=` 等）会隐式插入强制类型转换，而普通赋值不会。
