# 类加载

> 专题：JVM
> 来源：面试内容汇总/JVM.xmind

## 补充与实践

- 补充：类加载包括加载、验证、准备、解析、初始化，双亲委派主要用于保证核心类库一致性和安全边界。
- 实践：插件化、热部署、容器隔离会定制 ClassLoader，排查冲突时要确认同名类由哪个加载器加载。
- 误区：破坏双亲委派不是错误本身，关键是是否能控制类可见性和版本隔离。

## 脑图内容

## 类加载

### 生命周期

#### 加载

- 功能
  - 通过全类名获取定义此类的二进制字节流
  - 将字节流所代表的静态存储接口转换为方法区的运行时数据结构
  - 在内存中生成一个代表该类的Class对象，作为方法区这些数据的访问入口

- 双亲委派模型

- 子主题 3

- 每个类都有一个引用执行加载它的ClassLoader，数组类不是通过ClassLoader创建的，而是JVM在需要的时候自动创建的，数组通过getClassLoader()方法获取ClassLoader的时候和该数组的元素类型的ClassLoader是一致的

#### 验证

- -Xverify:none

- 文件格式验证
  - Class文件格式校验

- 元数据验证
  - 字节码语义检查

- 字节码验证
  - 程序语义检查

- 符号引用验证
  - 类的正确性检查

- 相关错误
  - IllegalAccessError
  - NoSuchFiledError
  - NoSuchMethodError

#### 准备

- 为类变量分配内存并设置类变量初始值，初始值为数据类型的默认值，而非代码中设置的初始值

#### 解析

- 虚拟机将常量池内的符号引用替换为直接引用的过程

- 主要处理的符号引用
  - 类
  - 接口
  - 字段
  - 类方法
  - 接口方法
  - 方法类型
  - 方法句柄
  - 调用限定符

#### 初始化

- 执行<clinit>()方法，线程安全的

- 必须初始化的场景
  - 遇到new，getstatic，putstatic,invokestatic4条指令时
  - 通过反射调用类时
  - 初始化一个类时，父类还未初始化
  - 虚拟机启动时需要顶一个执行的主类，虚拟机会初始化这个类
  - MethodHandle和VarHandle可以看做是轻量级反射调用，必须使用findStaticVarHandle来初始化要调用的类
  - 接口中加入了默认方法时，如果接口的实现类发生了初始化，则接口要在之前被初始化

#### 使用

#### 卸载

### 类加载器

#### 重要的加载器

- BootstrapClassLoader(启动类加载器)
  - 有C++实现

- ExtensionClassLoader(扩展类加载器)
  - Java9改成了PlatformClassLoader

- AppClassLoaader(应用程序类加载器)

#### 类判定相等规则

- 2个类的加载器相同，全类名相同
