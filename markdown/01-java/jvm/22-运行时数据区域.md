# 运行时数据区域

> 专题：JVM
> 来源：面试内容汇总/JVM.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `运行时数据区域` 是 `JVM` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `运行时数据区域` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `运行时数据区域`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `运行时数据区域` 展开，属于 `JVM` 的复习范围。
- 建议优先掌握：程序技术器, 虚拟机栈, 本地方法栈, 堆, 方法区/元空间, 运行时常量池, 字符串常量池, 直接内存。
- 二级节点提示了主要拆分角度：唯一一个不会出现OOM的区域, 栈如果允许动态扩展的，无法申请足够内存时出现OOM异常, 栈如果不允许动态扩展的，当请求栈的深度炒作最大深度时，抛StackOverFlowError, 虚拟机栈有一个个栈帧组成，栈帧由局部变量表、操作数栈、动态链接、方法返回地址组成, 本地方法执行的时候，在本地方法栈也会创建一个栈帧，用于存放该本地方法的局部变量，操作数栈，动态链接、出口信息。, 也会出现StackOverFlowError和OOM, OutOfMemoryError：GC Overhead Limit Exceeded, OurOfMemoryError:Java heap space, JDK8变动, 常见的符号引用。
- 三级节点通常对应具体细节、API、机制或易错点：当JVM花太多时间执行垃圾回收并且只能回收很少的堆空间时，就会发生此错误, 没有足够的空间存储新创建的对象, 元空间和运行时常量池放在了直接内存, 类, 字段, 方法, 接口。

### 复习追问

- `程序技术器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `虚拟机栈` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `本地方法栈` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `堆` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `方法区/元空间` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 运行时数据区域

### 程序技术器

#### 唯一一个不会出现OOM的区域

### 虚拟机栈

#### 栈如果允许动态扩展的，无法申请足够内存时出现OOM异常

#### 栈如果不允许动态扩展的，当请求栈的深度炒作最大深度时，抛StackOverFlowError

#### 虚拟机栈有一个个栈帧组成，栈帧由局部变量表、操作数栈、动态链接、方法返回地址组成

### 本地方法栈

#### 本地方法执行的时候，在本地方法栈也会创建一个栈帧，用于存放该本地方法的局部变量，操作数栈，动态链接、出口信息。

#### 也会出现StackOverFlowError和OOM

### 堆

#### OutOfMemoryError：GC Overhead Limit Exceeded

- 当JVM花太多时间执行垃圾回收并且只能回收很少的堆空间时，就会发生此错误

#### OurOfMemoryError:Java heap space

- 没有足够的空间存储新创建的对象

### 方法区/元空间

#### JDK8变动

- 元空间和运行时常量池放在了直接内存

### 运行时常量池

#### 常见的符号引用

- 类

- 字段

- 方法

- 接口

#### 存放在方法区

### 字符串常量池

#### StringTable实现

#### -XX:StringTableSize可以设定容量

#### JDK7永久代移到堆中

### 直接内存

#### 也会出现OutOfMemoryError
