# 类加载

> 专题：JVM
> 来源：JVM.xmind

## 补充与实践

- 补充：类加载包括加载、验证、准备、解析、初始化，双亲委派主要用于保证核心类库一致性和安全边界。
- 实践：插件化、热部署、容器隔离会定制 ClassLoader，排查冲突时要确认同名类由哪个加载器加载。
- 误区：破坏双亲委派不是错误本身，关键是是否能控制类可见性和版本隔离。

## 详细说明

### 是什么

- `类加载` 是 `JVM` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `类加载` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `类加载`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `类加载` 展开，属于 `JVM` 的复习范围。
- 建议优先掌握：类的生命周期, 双亲委派类加载器, 模块化类加载器。
- 二级节点提示了主要拆分角度：1、加载, 2、验证, 3、准备, 4、解析, 5、初始化, 6、使用, 7、卸载。
- 三级节点通常对应具体细节、API、机制或易错点：文件格式验证, 元数据验证, 字节码验证, 符号引用验证, 为类中的变量分配内存并设置初始值, 类或接口解析, 字段解析, 方法解析, 接口方法解析, cinit-类构造器。

### 复习追问

- `类的生命周期` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `双亲委派类加载器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `模块化类加载器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 类加载

### 类的生命周期

#### 1、加载

#### 2、验证

- 文件格式验证

- 元数据验证

- 字节码验证

- 符号引用验证

#### 3、准备

- 为类中的变量分配内存并设置初始值

#### 4、解析

- 类或接口解析

- 字段解析

- 方法解析

- 接口方法解析

#### 5、初始化

- cinit-类构造器

- init-实例构造器

#### 6、使用

#### 7、卸载

### 双亲委派类加载器

### 模块化类加载器
