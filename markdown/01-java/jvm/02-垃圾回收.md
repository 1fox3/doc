# 垃圾回收

> 专题：JVM
> 来源：JVM.xmind

## 补充与实践

- 补充：G1 是通用低停顿收集器，ZGC/Shenandoah 更适合大堆低延迟场景；CMS 已不再是新版本首选。
- 实践：GC 优化先确认目标是吞吐、停顿还是成本，再结合 GC 日志和对象分配热点调整。
- 排障：频繁 Full GC 常见原因包括老年代晋升压力、元空间膨胀、大对象分配和内存泄漏。

## 详细说明

### 是什么

- `垃圾回收` 是 `JVM` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `垃圾回收` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `垃圾回收`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `垃圾回收` 展开，属于 `JVM` 的复习范围。
- 建议优先掌握：对象回收判定, 垃圾收集算法, 算法细节, 经典垃圾收集器, 低延时收集器。
- 二级节点提示了主要拆分角度：引用计数, 可达性分析, 标记-清除, 标记-复制, 标记-整理, Gc Roots, 安全点, 安全区域, 记忆集与卡表, 写屏障。
- 三级节点通常对应具体细节、API、机制或易错点：增量更新, 原始快照, 垃圾收集时间占比, 1、初始标记, 2、并发标记, 3、重新标记, 4、并发清除, 3、最终标记, 4、筛选回收。

### 复习追问

- `对象回收判定` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `垃圾收集算法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `算法细节` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `经典垃圾收集器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `低延时收集器` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 垃圾回收

### 对象回收判定

#### 引用计数

#### 可达性分析

### 垃圾收集算法

#### 标记-清除

#### 标记-复制

#### 标记-整理

### 算法细节

#### Gc Roots

#### 安全点

#### 安全区域

#### 记忆集与卡表

#### 写屏障

#### 并发的可达性分析

- 增量更新

- 原始快照

### 经典垃圾收集器

#### Serial收集器

#### ParNew收集器

#### Parallel Scavenge收集器

- 垃圾收集时间占比

#### Serial Old收集器

#### Parallel Old收集器

#### CMS收集器(标记-清除)

- 1、初始标记

- 2、并发标记

- 3、重新标记

- 4、并发清除

#### G1(Garbage First)收集器

- 1、初始标记

- 2、并发标记

- 3、最终标记

- 4、筛选回收

### 低延时收集器

#### Shenandoah收集器

#### ZGC收集器
