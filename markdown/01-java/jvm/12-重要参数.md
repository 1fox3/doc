# 重要参数

> 专题：JVM
> 来源：面试内容汇总/JVM.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `重要参数` 是 `JVM` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `重要参数` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `重要参数`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `重要参数` 展开，属于 `JVM` 的复习范围。
- 建议优先掌握：堆内存, 新生代内存, 永久代, 元空间, 老年代转换, 栈, 垃圾收集器。
- 二级节点提示了主要拆分角度：-Xms, -Xmx, -Xmn, -XX:NewSize, -XX:MaxNewSize, -XX:NewRatio, -XX:ServivorRatio, -XX:PermSize, -XX:MaxPermSize, -XX:MetaspaceSize。
- 三级节点通常对应具体细节、API、机制或易错点：默认物理内存的1/64, 默认物理内存够的1/4, 新生代初始内存大小, 默认新生代占1/3，老生代占2/3, 默认Eden(8/10),From(1/10),To(1/10), 每个Region的大小, 期望达到的最大GC停顿时间默认200ms, 停顿的GC线程数，最大8个, 并发标记的线程, 触发并发GC周期的堆占用比率阈值，默认45%。

### 复习追问

- `堆内存` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `新生代内存` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `永久代` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `元空间` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `老年代转换` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 重要参数

### 堆内存

#### -Xms

- 默认物理内存的1/64

#### -Xmx

- 默认物理内存够的1/4

### 新生代内存

#### -Xmn

#### -XX:NewSize

- 新生代初始内存大小

#### -XX:MaxNewSize

#### -XX:NewRatio

- 默认新生代占1/3，老生代占2/3

#### -XX:ServivorRatio

- 默认Eden(8/10),From(1/10),To(1/10)

### 永久代

#### -XX:PermSize

#### -XX:MaxPermSize

### 元空间

#### -XX:MetaspaceSize

#### -XX:MaxMetaspaceSize

### 老年代转换

#### -XX:MaxTenuringThreshold

#### -XX:PretenureSizeThreshold

### 栈

#### -Xss

### 垃圾收集器

#### -XX:+UserSerialGC

#### -XX:+UserParallelGC

#### -XX:+UseParNewGC

#### -XX:+UseG1GC

- 每个Region的大小
  - -XX:G1HeapRegionSize

- 期望达到的最大GC停顿时间默认200ms
  - -XX:MaxGCPauseMills

- 停顿的GC线程数，最大8个
  - -XX:ParallelGCThread

- 并发标记的线程
  - -XX:ConcGCThreads

- 触发并发GC周期的堆占用比率阈值，默认45%
  - -XX:InitiatingHeapOccupancyPercent

#### -XX:+UseConcMarkSweepGC

- 并发的GC线程数
  - -XX:ConcGCThreads

- FullGC做压缩整理，减少碎片
  - -XX:+UseCMSCompactAtFullCollection

- FullGC压缩频次，默认为0，每次都会压缩
  - -XX:CMSFullGCsBefureCompaction

- 老年代触发FullGC的使用比例，默认92%
  - -XX:CMSInitatingOccupanyFraction

- 仅用设定的回收阈值，默认仅第一次使用设定值，后面会动态调整
  - -XX:+UseCMSInitiatingOccupancyOnly

- 在GC前执行一次MinorGC，用于减少老年代对年轻代的引用，降低标记阶段的开销
  - -XX:+CMSScavengeBeforeRemark

- 初始标记时以多线程执行，缩短停顿时间
  - -XX:+CMSParallelInitialMarkEnabled

- 重新标记时以多线程执行，缩短停顿时间
  - -XX:+CMSParallelRemarkEnable

### GC日志

#### 打印基本GC信息

- -XX:+PringGCDetail

- -XX:+PrintGCDateStamps

#### 打印对象分布

- -XX:+PrintTenuringDistribution

#### 打印堆数据

- -XX:+PrintHeapAtGC

#### 打印Referecnce处理因袭

- -XX:+PrintReferenceGC

#### 打印STW时间

- -XX:+PrintGCApplicationStoppedTime

#### 打印safepoint信息

- -XX:PrintSafepointStatistics

- -XX:PrintSagepointStatisticsCount=1

#### GC日志输出文件路径

- -Xloggc:/path/to/gc-%t.log

#### 开启日志分割

- -XX:+UseGCLogFileRotation

#### GC日志最多分割几个文件

- -XX:NumberOfGCLogFiles=14

#### 单个GC日志文件大小上限

- -XX:GCLogFIleSize=50M

### 处理OOM

#### 遇到OOM错误时将heap转储到物理文件

- -XX:+HeapDumpOnOutOfMemoryError

#### 要写入的文件路径

- -XX:+HeapDumpPath=.java_pid<pid>.hprof

#### 用于发出紧急指令

- -XX:OnOutOfMemoryError="<cmd args>;<cmd args>"

#### 限制在抛出OOM错误之前GC在VM中花费的时间比例

- -XX:+UseGCOverheadLimit
