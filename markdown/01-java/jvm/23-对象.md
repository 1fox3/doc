# 对象

> 专题：JVM
> 来源：面试内容汇总/JVM.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 是什么

- `对象` 是 `JVM` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `对象` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `对象`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `对象` 展开，属于 `JVM` 的复习范围。
- 建议优先掌握：对象创建, 对象的内存布局, 对象访问。
- 二级节点提示了主要拆分角度：类加载检查, 分配内存, 初始化零值, 设置对象头, 执行init方法, 对象头, 实例数据, 对其填充, 使用句柄, 直接指针。
- 三级节点通常对应具体细节、API、机制或易错点：遇到new指令时，首先定位检查常量池中类的符号引用，并检查类是否已被加载、解析和初始化过，如果没有则进行相应的类加载过程, 为对象分配内存空间, 内存分配后，虚拟机需要将分配的内存空间都初始化为0值，不包括对象头，以保证不符初始值就可以直接使用，程序能访问到字段的数据类型所对应的零值, 对对象进行必要的设置, 哈希吗, GC分代年龄, 锁状态标志, 类型指针, 句柄池, 对象被移动时只会改变句柄中的实例数据指针，而reference不需要修改。

### 复习追问

- `对象创建` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `对象的内存布局` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `对象访问` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 对象

### 对象创建

#### 类加载检查

- 遇到new指令时，首先定位检查常量池中类的符号引用，并检查类是否已被加载、解析和初始化过，如果没有则进行相应的类加载过程

#### 分配内存

- 为对象分配内存空间
  - 方式
    - 指针碰撞
    - 空闲列表
  - 内存分配并发问题
    - CAS+失败重试
    - TLAB，为每一个线程预先在Eden分配一块内存

#### 初始化零值

- 内存分配后，虚拟机需要将分配的内存空间都初始化为0值，不包括对象头，以保证不符初始值就可以直接使用，程序能访问到字段的数据类型所对应的零值

#### 设置对象头

- 对对象进行必要的设置

#### 执行init方法

### 对象的内存布局

#### 对象头

- 哈希吗

- GC分代年龄

- 锁状态标志

- 类型指针

#### 实例数据

#### 对其填充

### 对象访问

#### 使用句柄

- 句柄池

- 对象被移动时只会改变句柄中的实例数据指针，而reference不需要修改

#### 直接指针

- 最大的好处就是访问速度快，节省了一次指针定位的时间的开销
