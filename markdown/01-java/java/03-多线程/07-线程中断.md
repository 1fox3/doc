# 线程中断

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 是什么

- `线程中断` 是 `Java 语言基础` 体系中的一个具体知识点，建议先明确它解决的问题、参与的核心对象以及在整体链路中的位置。
- 如果原脑图只记录了标题，复习时应补齐定义、适用场景、关键流程和边界条件，避免面试时只能说概念名。

### 为什么重要

- 这类知识点通常会连接到源码机制、工程实践或线上故障，面试官更关注你能否把概念落到实际问题。
- 准备时不要只背结论，要能说明它和相邻知识点的区别、取舍以及常见误用。

### 实践关注

- 整理至少一个业务或框架中的使用场景。
- 补充常见坑点、异常表现、排查手段和优化方向。
- 如果涉及配置或参数，记录默认值、调优依据和风险边界。

### 面试表达

- 回答 `线程中断` 时，可以按“定义 -> 原理/流程 -> 使用场景 -> 常见问题 -> 项目经验”的顺序展开。
- 如果不确定细节，优先讲清边界和排查思路，不要把不同组件或不同版本的行为混在一起。

### 复习检查

- 能用自己的话解释 `线程中断`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `线程中断` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：相关方法, join方法, sleep和wait区别, yield, yield与sleep区别。
- 二级节点提示了主要拆分角度：Thread.interrupt, Thread.isInterrupted, Thread.interrupted, 等待这个线程结束，当前线程在继续执行, 利用了wait方法, 使用限制, 使用场景, 所属类, 释放锁, 线程切换。
- 三级节点通常对应具体细节、API、机制或易错点：中断目标线程，给目标线程发一个中断信号，线程被打上中断标记, 判断目标线程是否被中断，不会清除中断标记, 判断目标线程是否被中断，会清除中断标记, sleep方法可以让当前线程西面，时间一到当前线程继续之心，任何地方都能使用，需捕获InterruptedException异常, wait方法必须放在synchronized块里，同样需要捕获InterruptedException，需要配合notify进行唤醒，也可指定休眠实现, sleep一般用于当前线程西面，或者轮询操作暂停, wait多用于多线程之间的通信, sleep属于Thread, wait属于Object, sleep不会释放锁。

### 复习追问

- `相关方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `join方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `sleep和wait区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `yield` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `yield与sleep区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 线程中断

### 相关方法

#### Thread.interrupt

- 中断目标线程，给目标线程发一个中断信号，线程被打上中断标记

#### Thread.isInterrupted

- 判断目标线程是否被中断，不会清除中断标记

#### Thread.interrupted

- 判断目标线程是否被中断，会清除中断标记

### join方法

#### 等待这个线程结束，当前线程在继续执行

#### 利用了wait方法

### sleep和wait区别

#### 使用限制

- sleep方法可以让当前线程西面，时间一到当前线程继续之心，任何地方都能使用，需捕获InterruptedException异常

- wait方法必须放在synchronized块里，同样需要捕获InterruptedException，需要配合notify进行唤醒，也可指定休眠实现

#### 使用场景

- sleep一般用于当前线程西面，或者轮询操作暂停

- wait多用于多线程之间的通信

#### 所属类

- sleep属于Thread

- wait属于Object

#### 释放锁

- sleep不会释放锁

- wait可以释放当前线程对lock对象锁的持有

#### 线程切换

- sleep会强制上下文切换

- wait则不一定，wait后可能还有机会重新竞争到锁继续执行

### yield

#### Thread类的方法

#### 当前线程让出CPU的时间片，并变为就绪状态，重新竞争CPU的调度权。可能会获取到，也可能会被其他线程获取到

### yield与sleep区别

#### 相同点

- 暂停当前线程

- 暂停过程中如果已经持有锁，都不会释放锁资源

#### 不同点

- sleep可以制定具体的休眠时间，yield则依赖CPU的时间片划分

- yield不能被中断，而sleep则可以接受中断
