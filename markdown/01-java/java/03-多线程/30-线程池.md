# 线程池

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 核心参数

- `corePoolSize`：核心线程数。
- `maximumPoolSize`：最大线程数。
- `workQueue`：任务队列。
- `keepAliveTime`：非核心线程空闲存活时间。
- `RejectedExecutionHandler`：拒绝策略。

### 执行流程

- 线程数小于 core 时创建核心线程。
- 核心线程满后任务进入队列。
- 队列满后创建非核心线程，直到 maximum。
- 线程和队列都满时触发拒绝策略。

### 实践建议

- CPU 密集型任务线程数接近 CPU 核数。
- IO 密集型任务可适当增大线程数，但要受下游容量约束。
- 不要使用无界队列承接不可控流量，否则可能导致内存膨胀和延迟失控。

### 复习检查

- 能用自己的话解释 `线程池`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `线程池` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：创建线程池, 内置线程池, ThreadPoolExecutor, 线程池命名。
- 二级节点提示了主要拆分角度：通过ThreadPoolExecutor构造函数来创建, Executor框架的工具类Executors来创建, FixedThreadPool, SingleThreadExecutor, CacheThreadPool, ScheduledThreadPool, SingleThreadScheduledExecutor, corePoolSize, maximumPoolSize, workQueue。
- 三级节点通常对应具体细节、API、机制或易错点：LinkedBlockingQueue, SynchronousQueue, DelayedWorkQueue, AbortPolicy, CallerRunsPolicy, DiscardPolicy, DiscardOldestPolicy。

### 复习追问

- `创建线程池` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `内置线程池` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `ThreadPoolExecutor` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `线程池命名` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 线程池

### 创建线程池

#### 通过ThreadPoolExecutor构造函数来创建

#### Executor框架的工具类Executors来创建

### 内置线程池

#### FixedThreadPool

- LinkedBlockingQueue

#### SingleThreadExecutor

- LinkedBlockingQueue

#### CacheThreadPool

- SynchronousQueue

#### ScheduledThreadPool

- DelayedWorkQueue

#### SingleThreadScheduledExecutor

- DelayedWorkQueue
  - 按照计划执行时间进行排序

### ThreadPoolExecutor

#### corePoolSize

#### maximumPoolSize

#### workQueue

#### keepAliveTime

#### unit

#### threadFactory

#### handler

- AbortPolicy

- CallerRunsPolicy

- DiscardPolicy

- DiscardOldestPolicy

### 线程池命名

#### 利用guava的ThreadFactoryBuilder,本质上也是自定义ThreadFactory

#### 自己实现ThreadFactory，创建线程时调用thread.setName
