# 线程终止

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 推荐方式

- 使用共享停止标志，例如 volatile boolean。
- 使用 `interrupt` 中断阻塞或协作式停止。
- 线程池使用 `shutdown` 或 `shutdownNow`，并配合超时等待。

### 不推荐方式

- 不要使用 `Thread.stop`，它会强制释放锁，可能破坏对象一致性。

### 复习检查

- 能用自己的话解释 `线程终止`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `线程终止` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：stop, 在逻辑中增加线程停止开关逻辑。
- 二级节点提示了主要拆分角度：释放锁并强制终止线程，不推荐使用, volatile 变量。

### 复习追问

- `stop` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `在逻辑中增加线程停止开关逻辑` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 线程终止

### stop

#### 释放锁并强制终止线程，不推荐使用

### 在逻辑中增加线程停止开关逻辑

#### volatile 变量
