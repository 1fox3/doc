# ReentrantReadWriteLock

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 核心概念

- 读写锁将读和写分离，允许多个读线程并发，写线程独占。
- 适合读多写少且读操作耗时明显的场景。

### 注意点

- 写锁可以降级为读锁，读锁不能直接升级为写锁，否则可能死锁。
- 如果写操作频繁，读写锁收益可能不明显，甚至更复杂。

### 复习检查

- 能用自己的话解释 `ReentrantReadWriteLock`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `ReentrantReadWriteLock` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：可重入锁, 实现了读写锁ReadWriteLock, 读读不互斥，读写、写写互斥。

### 复习追问

- `可重入锁` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `实现了读写锁ReadWriteLock` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `读读不互斥，读写、写写互斥` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## ReentrantReadWriteLock

### 可重入锁

### 实现了读写锁ReadWriteLock

### 读读不互斥，读写、写写互斥
