# 线程顺序执行的方法

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 常见方式

- 使用 `Thread#join` 等待前一个线程结束。
- 使用 `CountDownLatch` 控制阶段顺序。
- 使用单线程线程池保证任务按提交顺序执行。
- 使用 `CompletableFuture` 编排依赖关系。

### 实践建议

- 如果只是任务编排，优先使用线程池和 Future/CompletableFuture，而不是手动创建多个 Thread。

### 复习检查

- 能用自己的话解释 `线程顺序执行的方法`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `线程顺序执行的方法` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：使用线程的join方法, 使用对象的wait方法, 使用重入锁Condition的await方法, SingleThreadExecutor，单线程池。

### 复习追问

- `使用线程的join方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `使用对象的wait方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `使用重入锁Condition的await方法` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `SingleThreadExecutor，单线程池` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 线程顺序执行的方法

### 使用线程的join方法

### 使用对象的wait方法

### 使用重入锁Condition的await方法

### SingleThreadExecutor，单线程池
