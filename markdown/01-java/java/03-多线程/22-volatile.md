# volatile

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 核心语义

- 可见性：一个线程修改 volatile 变量后，其他线程能及时看到。
- 有序性：volatile 读写会插入内存屏障，限制指令重排序。
- 不保证复合操作原子性，例如 `i++` 仍不是线程安全。

### 适用场景

- 状态标记，例如线程停止标志。
- 双重检查锁中的单例引用，防止对象未初始化完成就被其他线程看到。
- 一写多读且写入不依赖旧值的场景。

### 面试重点

- volatile 解决可见性和有序性，不解决互斥。需要互斥修改共享状态时应使用 synchronized、Lock 或原子类。

### 复习检查

- 能用自己的话解释 `volatile`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `volatile` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：保证数据可见性，不能保证数据原子性, 通过插入特定的内存屏障方式来禁止指令重排。

### 复习追问

- `保证数据可见性，不能保证数据原子性` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `通过插入特定的内存屏障方式来禁止指令重排` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## volatile

### 保证数据可见性，不能保证数据原子性

### 通过插入特定的内存屏障方式来禁止指令重排
