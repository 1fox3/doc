# 守护线程

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：JDK 21 的虚拟线程适合大量阻塞 IO 场景，但不适合 CPU 密集型任务，也不能消除共享状态同步问题。
- 实践：线程池参数应结合任务类型、队列长度、拒绝策略、监控指标和下游容量一起设计。
- 排障：线程问题优先看 `jstack`、线程池队列、活跃线程数、锁等待和上下文切换。

## 详细说明

### 核心概念

- 守护线程是为用户线程服务的后台线程。
- 当 JVM 中只剩守护线程时，JVM 会退出。
- 典型守护线程包括 GC 相关线程、监控或后台清理线程。

### 注意点

- 守护线程不适合承载必须完成的数据写入、文件关闭、事务提交等关键任务。
- 必须在线程启动前调用 `setDaemon(true)`。

### 复习检查

- 能用自己的话解释 `守护线程`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `守护线程` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：伴随用户线程，用户线程结束，守护线程才结束, Trhead类的daemon成员变量设置为true，默认为false。

### 复习追问

- `伴随用户线程，用户线程结束，守护线程才结束` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Trhead类的daemon成员变量设置为true，默认为false` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 守护线程

### 伴随用户线程，用户线程结束，守护线程才结束

### Trhead类的daemon成员变量设置为true，默认为false
