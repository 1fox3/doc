# 序列化

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 核心概念

- 序列化是把对象转换为可存储或可传输格式，反序列化是把数据恢复为对象。
- Java 原生序列化依赖 `Serializable`，但性能、安全性和跨语言能力较弱。

### 常见方案

- JSON：可读性好，常用于 HTTP API，但体积较大。
- Protobuf：体积小、跨语言、性能好，适合 RPC 和消息。
- Kryo/Hessian：常见于 Java 生态 RPC 或缓存场景。

### 风险

- 反序列化不可信数据可能造成安全漏洞。
- 类结构变更会带来兼容性问题，需要设计 schema 演进策略。

### 复习检查

- 能用自己的话解释 `序列化`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `序列化` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：transient, static变量不属于是任何对象，无论有没有transient修饰，都不会被实例化, serialVersionUID。
- 二级节点提示了主要拆分角度：只能修饰变量，不能修饰类和方法, 被修饰的变量，反序列化时会被置成默认值, 属于版本控制作用。

### 复习追问

- `transient` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `static变量不属于是任何对象，无论有没有transient修饰，都不会被实例化` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `serialVersionUID` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 序列化

### transient

#### 只能修饰变量，不能修饰类和方法

#### 被修饰的变量，反序列化时会被置成默认值

### static变量不属于是任何对象，无论有没有transient修饰，都不会被实例化

### serialVersionUID

#### 属于版本控制作用
