# JDK8提供的内置的函数式接口

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 常见函数式接口

- `Function<T,R>`：接收 T，返回 R。
- `Consumer<T>`：接收 T，无返回值。
- `Supplier<T>`：无入参，返回 T。
- `Predicate<T>`：接收 T，返回 boolean。
- `UnaryOperator<T>`：接收 T，返回 T。
- `BinaryOperator<T>`：接收两个 T，返回 T。

### 示例代码

```java
Function<String, Integer> length = String::length;
Predicate<String> notBlank = s -> s != null && !s.isBlank();
Consumer<String> printer = System.out::println;
Supplier<Long> now = System::currentTimeMillis;

if (notBlank.test("java")) {
    printer.accept("len=" + length.apply("java"));
}
```

### 复习检查

- 能用自己的话解释 `JDK8提供的内置的函数式接口`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `JDK8提供的内置的函数式接口` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：Consumer<T>, Supplier<T>, Predicate<T>, Function<T, R>, BinaryOperator<T>。
- 二级节点提示了主要拆分角度：T最为输入，没有返回值, 没有输入，返回T, T作为输入，返回boolean, T作为输入，R作为输出, 两个T作为输入，放回一个T作为输入。

### 复习追问

- `Consumer<T>` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Supplier<T>` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Predicate<T>` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `Function<T, R>` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `BinaryOperator<T>` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## JDK8提供的内置的函数式接口

### Consumer<T>

#### T最为输入，没有返回值

### Supplier<T>

#### 没有输入，返回T

### Predicate<T>

#### T作为输入，返回boolean

### Function<T, R>

#### T作为输入，R作为输出

### BinaryOperator<T>

#### 两个T作为输入，放回一个T作为输入
