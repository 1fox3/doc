# 语法糖

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：现代 Java 基础题更关注语义边界，例如值传递、不可变对象、自动装箱缓存、字符串常量池和泛型擦除。
- 实践：编码时优先使用清晰的不可变模型和显式类型边界，避免依赖隐式转换或包装类型比较。
- 误区：`final` 只能保证引用不可变，不能保证引用对象内部状态不可变。

## 脑图内容

## 语法糖

### switch支持字符串

#### 通过hashCode和equals实现

### 字符串+拼接

### 泛型与类型擦除

### 自动装箱与拆箱

### 变长参数

### 增强for循环

### 内部类

#### OutterClass$InnerClass.class

### 枚举类

#### 使用enum定义一个枚举类型时，编译器会自动创建一个final类型继承Enum类，所以枚举类不能被继承

### 断言

### lambda表达式

#### LambdaMetafactory.metafactory

### try-with-resources

### var类型推断
