# SPI

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 核心概念

- SPI 是 Service Provider Interface，用于面向接口发现实现类。
- JDK SPI 通过 `META-INF/services/接口全限定名` 配置实现类，再用 `ServiceLoader` 加载。

### 应用场景

- JDBC Driver、日志框架、Dubbo 扩展点、序列化插件等都体现了 SPI 思想。

### 注意点

- JDK SPI 会一次性加载并实例化实现类，按需加载和扩展能力相对较弱；Dubbo SPI 做了更丰富的增强。

### 复习检查

- 能用自己的话解释 `SPI`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `SPI` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：SPI机制的具体实现本质上是通过反射完成的, 我们按照规定，将要暴露的具体实现类放在META-INF/services/文件下声明, ServiceLoader负责加载配置文件，并通过反射的方式实例化具体的类。

### 复习追问

- `SPI机制的具体实现本质上是通过反射完成的` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `我们按照规定，将要暴露的具体实现类放在META-INF/services/文件下声明` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `ServiceLoader负责加载配置文件，并通过反射的方式实例化具体的类` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## SPI

### SPI机制的具体实现本质上是通过反射完成的

### 我们按照规定，将要暴露的具体实现类放在META-INF/services/文件下声明

### ServiceLoader负责加载配置文件，并通过反射的方式实例化具体的类
