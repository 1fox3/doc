# 跳出多层循环

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 常见方式

- 使用带标签的 `break label` 跳出指定外层循环。
- 将循环逻辑抽成方法，通过 `return` 提前结束。
- 使用状态变量控制外层循环，但可读性通常较差。

### 实践建议

- 业务代码中不要滥用 label，复杂循环更建议拆函数提升可读性。

### 示例代码

```java
public class BreakOuterLoopDemo {
    public static void main(String[] args) {
        outer:
        for (int i = 0; i < 3; i++) {
            for (int j = 0; j < 3; j++) {
                System.out.println("i=" + i + ", j=" + j);

                if (i == 1 && j == 1) {
                    break outer; // 跳出 outer 标记的外层循环
                }
            }
        }

        System.out.println("end");
    }
}
```

### 复习检查

- 能用自己的话解释 `跳出多层循环`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `跳出多层循环` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：在最外面的循环语句前定义一个标号，然后通过break 标号语句跳出嵌套循环。

### 复习追问

- `在最外面的循环语句前定义一个标号，然后通过break 标号语句跳出嵌套循环` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 跳出多层循环

### 在最外面的循环语句前定义一个标号，然后通过break 标号语句跳出嵌套循环
