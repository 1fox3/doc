# 包装类

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：现代 Java 基础题更关注语义边界，例如值传递、不可变对象、自动装箱缓存、字符串常量池和泛型擦除。
- 实践：编码时优先使用清晰的不可变模型和显式类型边界，避免依赖隐式转换或包装类型比较。
- 误区：`final` 只能保证引用不可变，不能保证引用对象内部状态不可变。

## 详细说明

### 核心概念

- 包装类是基本类型的对象形式，例如 `int` 对应 `Integer`，`long` 对应 `Long`。
- 包装类可用于泛型、集合、反射、空值表达等基本类型不能直接使用的场景。

### 缓存机制

- `Integer` 默认缓存 `-128` 到 `127` 范围内的对象。
- `Byte`、`Short`、`Long`、`Character` 也有类似缓存范围。
- 不要依赖 `==` 比较包装类数值，应使用 `equals` 或转成基本类型。

### 拆箱 NPE

- 包装类型为 null 时自动拆箱会抛出 `NullPointerException`。
- 数据库字段、JSON 入参、RPC DTO 中的包装类型要特别注意 null。

### 示例代码

```java
Integer a = 127;
Integer b = 127;
System.out.println(a == b); // true，命中缓存

Integer c = 128;
Integer d = 128;
System.out.println(c == d); // false，通常未命中缓存

Integer value = null;
// int n = value; // 自动拆箱会抛 NullPointerException
```

### 复习检查

- 能用自己的话解释 `包装类`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `包装类` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：自动装箱、拆箱, 包装类缓存, 基本类和包装类的区别。
- 二级节点提示了主要拆分角度：装箱, 拆箱, valueOf才会用到, 用途, 存储方式, 占用空间, 默认值, 比较方式。
- 三级节点通常对应具体细节、API、机制或易错点：调用为valueOf, 调用xxxValue方法, 除了定义一些常量和局部变量之外，我们在其他地方比如方法参数、对象属性中很少使用基本类型来定义变量。并且包装类可用于泛型，基本类型不可以, 基本数据类型的局部变量存放在java虚拟机栈中的局部变量表中，基本类型的成员变量(未被static修饰)存放在java虚拟机的堆中。包装类属于对象类型，几乎都存在堆中, 基本类型相比于包装类所占空间相对很小, 包装类默认值是null，基本类型默认值不是null, 基本类型通过==比较的是值，包装类==比较的是对象的内存地址。包装类对象之间的比较使用equals。

### 复习追问

- `自动装箱、拆箱` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `包装类缓存` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `基本类和包装类的区别` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 包装类

### 自动装箱、拆箱

#### 装箱

- 调用为valueOf

#### 拆箱

- 调用xxxValue方法

### 包装类缓存

#### valueOf才会用到

### 基本类和包装类的区别

#### 用途

- 除了定义一些常量和局部变量之外，我们在其他地方比如方法参数、对象属性中很少使用基本类型来定义变量。并且包装类可用于泛型，基本类型不可以

#### 存储方式

- 基本数据类型的局部变量存放在java虚拟机栈中的局部变量表中，基本类型的成员变量(未被static修饰)存放在java虚拟机的堆中。包装类属于对象类型，几乎都存在堆中

#### 占用空间

- 基本类型相比于包装类所占空间相对很小

#### 默认值

- 包装类默认值是null，基本类型默认值不是null

#### 比较方式

- 基本类型通过==比较的是值，包装类==比较的是对象的内存地址。包装类对象之间的比较使用equals
