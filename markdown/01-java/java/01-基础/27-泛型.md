# 泛型

> 专题：Java 语言基础
> 来源：面试内容汇总/Java.xmind

## 补充与实践

- 补充：建议从问题背景、核心原理、适用场景、边界条件和生产案例五个维度整理该知识点。
- 实践：学习时结合监控指标、日志、配置参数和故障复盘，避免只停留在概念记忆。

## 详细说明

### 核心概念

- 泛型用于把类型参数化，让编译器在编译期做类型检查。
- Java 泛型通过类型擦除实现，运行期大部分泛型信息不可见。

### 通配符

- `? extends T`：上界通配符，适合读取，表示 T 或 T 的子类。
- `? super T`：下界通配符，适合写入，表示 T 或 T 的父类。
- 口诀 PECS：Producer Extends，Consumer Super。

### 常见限制

- 不能直接 `new T()`。
- 不能创建泛型数组，例如 `new List<String>[10]`。
- 运行期不能直接用 `instanceof List<String>` 判断具体泛型参数。

### 复习检查

- 能用自己的话解释 `泛型`，而不是只复述标题。
- 能说出至少一个生产场景、一个常见坑点和一个排查方向。

## 脑图解读

- 本节脑图围绕 `泛型` 展开，属于 `Java 语言基础` 的复习范围。
- 建议优先掌握：分类, 标识符。
- 二级节点提示了主要拆分角度：泛型类, 泛型接口, 泛型方法, T（Type-类型）, R(Result-结果), K（Key-键）, V（Value-值）, E(Element-元素), N(Number-数字), ?(通配符，不确定类型)。

### 复习追问

- `分类` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？
- `标识符` 解决什么问题？核心机制是什么？有什么常见坑或替代方案？

## 脑图内容

## 泛型

### 分类

#### 泛型类

#### 泛型接口

#### 泛型方法

### 标识符

#### T（Type-类型）

#### R(Result-结果)

#### K（Key-键）

#### V（Value-值）

#### E(Element-元素)

#### N(Number-数字)

#### ?(通配符，不确定类型)
